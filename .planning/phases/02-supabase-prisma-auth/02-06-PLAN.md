---
phase: 02-supabase-prisma-auth
plan: 06
type: execute
wave: 5
depends_on: [04, 05]
files_modified:
  - src/app/(auth)/signup/actions.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - .env.example
  - .planning/codebase/ARCHITECTURE.md
autonomous: false
requirements:
  - AUTH-01
tags: [kakao, oauth, verification, smoke-test, phase-close]

must_haves:
  truths:
    - "`signInWithKakao` Server Action이 `signInWithOAuth({ provider: 'kakao' })`로 작동한다 (RESEARCH.md §Key Finding #1: Supabase 내장 provider, Custom OIDC 불필요)"
    - "login과 signup 페이지에 Kakao 로그인 버튼이 추가되어 있다 (시각 디자인은 기존 Google 버튼과 동일 스타일)"
    - "Supabase Dashboard에 Kakao provider가 enable되어 있음이 사용자 confirm 완료 (checkpoint)"
    - "Kakao Developers dashboard에 redirect URI가 `https://<project>.supabase.co/auth/v1/callback`로 등록되어 있음이 사용자 confirm 완료"
    - "tests/auth/kakao-oauth.test.ts가 녹색으로 통과한다 (data.url starts with https://kauth.kakao.com/oauth/authorize?)"
    - "Full Phase 2 E2E smoke 실행: 6개 dev 계정 모두 로그인 → role-gated landing → logout 흐름 완주"
    - "`tests/e2e/session-persist.spec.ts`, `tests/e2e/logout.spec.ts`, `tests/e2e/protected-redirect.spec.ts`, `tests/e2e/role-worker-only.spec.ts`, `tests/e2e/role-biz-only.spec.ts` 5개 모두 GREEN"
    - "AUTH-01 drift note가 STATE.md Known Risks에 기록되어 있다 (이메일 전용 구현, SMS OTP는 v2)"
    - "ARCHITECTURE.md 드리프트가 Phase 2 종료 시점 상태로 최종 수정되어 있다 (Plan 01이 시작한 수정 + Phase 2 완료 상태 반영)"
  artifacts:
    - path: src/app/(auth)/signup/actions.ts
      provides: "signInWithKakao added to existing signup actions file"
      contains: "signInWithKakao"
    - path: src/app/(auth)/login/page.tsx
      provides: "Kakao login button wired to signInWithKakao"
      contains: "Kakao"
    - path: .env.example
      provides: "SUPABASE_AUTH_KAKAO_CLIENT_ID + SECRET already present from Plan 01"
      contains: "SUPABASE_AUTH_KAKAO_CLIENT_ID"
  key_links:
    - from: "login page Kakao button"
      to: "signInWithKakao server action"
      via: "form action"
      pattern: "action=\\{signInWithKakao\\}"
    - from: "Supabase Dashboard Kakao provider"
      to: "Kakao Developers app"
      via: "REST API key + secret pasted into Supabase provider config"
---

<objective>
Phase 2의 마지막 마무리 — Kakao OAuth 추가 + 전체 Phase 2 E2E smoke test + drift note + ARCHITECTURE.md 최종 확인.

**왜 Kakao가 마지막 wave인가:** CONTEXT.md D-01 + RESEARCH.md §Key Finding #1에서 "기본 3종(Password/MagicLink/Google)이 작동한 뒤 추가" 순서가 잠겨있다. Plan 04가 3종을 wire하고, Plan 05가 seed로 계정을 만든 뒤, Plan 06가 **이미 작동하는 기반 위에 Kakao를 얹는다** — 같은 `signInWithOAuth` helper에 provider만 바꾸면 된다.

**리서치의 중요한 발견:** CONTEXT.md D-01은 Kakao를 "Supabase Custom OIDC provider로 구현"이라고 가정했지만, 02-RESEARCH.md §Key Finding #1 (line 12-13)에서 **이것이 틀렸음이 확인됐다**. Kakao는 Supabase 내장 provider다. 따라서 Plan 06 구현은 극도로 단순하다 — Server Action 한 줄 추가 + 버튼 하나 + Dashboard 토글.

**출력:** (1) `signInWithKakao` action (signup/actions.ts에 추가), (2) login + signup 페이지에 Kakao 버튼, (3) Dashboard 토글 checkpoint, (4) 5개 E2E 테스트 GREEN, (5) STATE.md drift note, (6) ARCHITECTURE.md 최종 확인.

**Phase 2 종료 조건:** 이 Plan이 끝나면 `/gsd-verify-work 2`가 호출되어도 모든 REQ-ID가 GREEN이어야 한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-prisma-auth/02-CONTEXT.md
@.planning/phases/02-supabase-prisma-auth/02-RESEARCH.md
@.planning/phases/02-supabase-prisma-auth/02-VALIDATION.md
@.planning/phases/02-supabase-prisma-auth/02-04-SUMMARY.md
@.planning/phases/02-supabase-prisma-auth/02-05-SUMMARY.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@AGENTS.md
@CLAUDE.md
@src/app/(auth)/signup/actions.ts
@src/app/(auth)/login/page.tsx
@src/app/(auth)/signup/page.tsx
@tests/auth/kakao-oauth.test.ts
@tests/e2e/session-persist.spec.ts
@tests/e2e/logout.spec.ts
@tests/e2e/protected-redirect.spec.ts
@tests/e2e/role-worker-only.spec.ts
@tests/e2e/role-biz-only.spec.ts

<interfaces>
Consumed from Plan 04:
- `src/app/(auth)/signup/actions.ts` exports `signInWithGoogle` (pattern to replicate for Kakao)
- Login page has Google button wired; Kakao button is added next to it
- `/auth/callback/route.ts` has `ALLOWED_NEXT_PATHS` allowlist — handles both Google AND Kakao callbacks (same logic)

Consumed from Plan 05:
- 6 dev accounts in `auth.users` + `public.users`
- Password for all: `gignowdev`
- Smoke test login targets: worker+test, business+test, both+test

Created by this Plan:
- `signInWithKakao()` Server Action (same shape as signInWithGoogle)
- Kakao login button in login page
- Kakao signup button in signup page

External config (user performs via Supabase Dashboard + Kakao Developers):
- Supabase Dashboard → Authentication → Providers → Kakao → enable + paste Kakao REST API key + client secret
- Kakao Developers → My Applications → <app> → Platforms → Web → Site Domain = `https://<project>.supabase.co`, Redirect URI = `https://<project>.supabase.co/auth/v1/callback`
- Kakao Developers → Kakao Login → Activate
</interfaces>

<anti_patterns>
- **NEVER** implement Kakao as a custom OIDC provider — RESEARCH.md Key Finding #1 proved this is wrong. Use `signInWithOAuth({ provider: 'kakao' })` one-liner.
- **NEVER** set Kakao Developers redirect URI to the app's own `/auth/callback` — Kakao must call Supabase's `/auth/v1/callback`, which then calls the app's `/auth/callback`. RESEARCH.md Pitfall #9 L1354-L1358.
- **NEVER** put Kakao REST API key in `.env.local` — it goes into Supabase Dashboard only (Supabase stores it on the server side).
- **NEVER** skip the E2E smoke test for all 6 accounts — Phase 2 exit criterion from CONTEXT.md boundary section requires it.
- **NEVER** redesign the login/signup UI — Phase 2 is wire-up only. Kakao button uses same Button variant as existing Google button.
- **NEVER** assume AUTH-01's "phone or email" requirement is fully satisfied — email-only per D-01. Must record drift note in STATE.md.
- Do NOT install Auth.js / Clerk — locked.
- Do NOT add SMS OTP — deferred to v2 per CONTEXT.md Deferred Ideas.
</anti_patterns>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User enables Kakao provider in Supabase Dashboard + configures Kakao Developers app</name>
  <what-built>
    Nothing yet — this is a manual prerequisite that ONLY the user can complete. Kakao Developers requires an actual Korean Kakao account to create an application. Claude cannot automate this.
  </what-built>
  <how-to-verify>
    **Step 1 — Create or select Kakao Developers app:**
    1. Go to https://developers.kakao.com
    2. Log in with Korean Kakao account
    3. Navigate to "My Applications" → either create a new app or select existing dev app
    4. Note the **REST API key** (this is what Supabase calls "Kakao Client ID")
    5. Navigate to "Security" tab → generate a **Client Secret** (Activate state: ON)

    **Step 2 — Configure redirect URI in Kakao Developers:**
    1. In the same app, go to "Platform" → "Web"
    2. Add Site Domain: `https://<project-ref>.supabase.co` (replace <project-ref> with your actual project ref from .env.local NEXT_PUBLIC_SUPABASE_URL)
    3. Go to "Kakao Login" tab
    4. Click "Activate" (toggle ON)
    5. Under "Redirect URI", add: `https://<project-ref>.supabase.co/auth/v1/callback`
    6. Under "Consent Items", set "profile_nickname" and "account_email" to "Required consent" (필수 동의)

    **Step 3 — Enable Kakao in Supabase Dashboard:**
    1. Go to Supabase Dashboard → your project → Authentication → Providers
    2. Find "Kakao" in the list
    3. Click to expand → toggle "Enable Kakao Provider" ON
    4. Paste "REST API key" from Step 1 into Supabase's "Kakao Client ID" field
    5. Paste "Client Secret" from Step 1 into Supabase's "Kakao Client Secret" field
    6. Save

    **Step 4 — Confirm back to Claude:**
    Report to Claude: (a) Kakao provider enabled: yes/no, (b) redirect URI configured: yes/no, (c) Any errors encountered. Claude will proceed with Task 2 only after `yes` on both.

    **If you do not have a Kakao Developers account** or don't want to deal with this now, reply `defer-kakao`. Claude will skip Tasks 2+3 and mark Kakao as "Phase 2 optional, deferred to post-MVP" — Phase 2 can still be considered complete because CONTEXT.md D-01 makes Kakao a "bonus" wave.

    **Required env var updates (user does locally if they want to document):**
    You can optionally add these to `.env.local` for local reference (they are NOT required at runtime — Supabase stores them):
    - `SUPABASE_AUTH_KAKAO_CLIENT_ID=<REST API key>`
    - `SUPABASE_AUTH_KAKAO_SECRET=<Client Secret>`
  </how-to-verify>
  <resume-signal>Reply `kakao-ready`, `defer-kakao`, or describe issues encountered.</resume-signal>

  <files>(checkpoint — external configuration: Supabase Dashboard + Kakao Developers; no local file writes)</files>
  <action>CHECKPOINT: Blocking human-action checkpoint. Executor must halt and present the what-built, how-to-verify, and resume-signal XML blocks above to the user. The user performs external dashboard steps (Kakao Developers app creation, Supabase Dashboard Kakao provider toggle). Executor resumes only after user replies with kakao-ready or defer-kakao.</action>
  <verify>
    <automated>echo 'checkpoint: user external configuration required — no automated verify'</automated>
  </verify>
  <done>User replied kakao-ready (provider enabled) or defer-kakao (Kakao skipped for this phase). Decision recorded in 02-06-SUMMARY.md Task 1 section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add signInWithKakao Server Action + Kakao buttons on login + signup pages</name>
  <files>src/app/(auth)/signup/actions.ts, src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx</files>
  <behavior>
    - `tests/auth/kakao-oauth.test.ts` turns GREEN — asserts `signInWithKakao()` returns redirect URL starting with `https://kauth.kakao.com/oauth/authorize?`
    - Login page has a Kakao button form next to the Google button, visually consistent
    - Signup page has a Kakao button form next to the Google button
    - `signInWithKakao` Server Action is a minimal clone of `signInWithGoogle` (already in Plan 04 signup/actions.ts) — only the `provider` string changes
    - **Skip this task entirely** if Task 1 resolved with `defer-kakao` — mark task done with a note in SUMMARY
  </behavior>
  <read_first>
    - 02-RESEARCH.md lines 491-502 (Pattern 4 signInWithKakao verbatim — exact one-liner clone of signInWithGoogle)
    - 02-RESEARCH.md lines 11-13 (Key Finding #1 — Kakao is built-in provider, not custom OIDC)
    - 02-RESEARCH.md lines 1354-1358 (Pitfall #9 — Kakao redirect URI confusion)
    - src/app/(auth)/signup/actions.ts (Plan 04 created — signInWithGoogle pattern to clone)
    - src/app/(auth)/login/page.tsx (Plan 04 wired — add Kakao button next to Google)
    - src/app/(auth)/signup/page.tsx (Plan 04 wired — add Kakao button)
    - tests/auth/kakao-oauth.test.ts (Plan 01 stub — this task makes it green)
  </read_first>
  <action>
    **Step 1 — Append `signInWithKakao` to `src/app/(auth)/signup/actions.ts`** (this is where Google lives per Plan 04). Add after `signInWithGoogle`:
    ```ts
    // Phase 2 Wave 5 — Kakao built-in Supabase provider (RESEARCH.md §Key Finding #1)
    // Kakao is a built-in provider, NOT a custom OIDC. Just toggle on Supabase Dashboard.
    export async function signInWithKakao() {
      // Re-verify N/A: pre-session; OAuth redirect creates the session on callback.
      const supabase = await createClient()
      const { data, error } = await supabase.auth.signInWithOAuth({
        provider: 'kakao',
        options: {
          redirectTo: `${process.env.NEXT_PUBLIC_APP_URL}/auth/callback?next=/role-select`,
        },
      })
      if (error) return { error: { form: [error.message] } }
      if (data.url) redirect(data.url)
    }
    ```

    Note: same shape as `signInWithGoogle`, only `provider: 'kakao'`. The `/auth/callback` route already handles the code exchange with allowlisted next param (Plan 04 Task 2).

    **Step 2 — Add Kakao button to `src/app/(auth)/login/page.tsx`.** Find the existing Google button form (added in Plan 04 Task 2) and add a Kakao form directly after it:
    ```tsx
    <form action={signInWithKakao}>
      <Button
        type="submit"
        variant="outline"
        className="w-full bg-[#FEE500] hover:bg-[#FDD835] text-[#191919] border-[#FEE500]"
      >
        카카오로 계속하기
      </Button>
    </form>
    ```

    The Kakao yellow color `#FEE500` is the official brand color; the design change is limited to this single button's background.

    Import at the top:
    ```tsx
    import { signInWithKakao } from '@/app/(auth)/signup/actions';
    // (signInWithGoogle is already imported from Plan 04 Task 2)
    ```

    **Step 3 — Add Kakao button to `src/app/(auth)/signup/page.tsx`.** Same pattern — find the existing Google button and add Kakao button next to it.

    **Step 4 — Run test stub.** `tests/auth/kakao-oauth.test.ts` should now be unblocked. Remove the `describe.skip` wrapper if Plan 01 put one in (the stub was marked "Wave 5 activates this"). Run:
    ```
    npx vitest run tests/auth/kakao-oauth.test.ts --reporter=dot
    ```
    Expected: GREEN. The test asserts `data.url` starts with `https://kauth.kakao.com/oauth/authorize?`.

    **If Task 1 resolved with `defer-kakao`:** Skip all of this task. Instead, create a note in SUMMARY saying "Kakao deferred by user in Task 1. To enable later, follow Plan 06 Task 2 steps above." Mark `tests/auth/kakao-oauth.test.ts` as permanent `test.skip` with comment referencing this Plan.

    **Anti-pattern guard:**
    - Do NOT create a new actions file for Kakao — append to existing signup/actions.ts
    - Do NOT implement a custom OIDC flow — Supabase handles Kakao natively
    - Do NOT add Kakao-specific OAuth scopes — default (profile_nickname, account_email) is configured in Kakao Dashboard
    - Do NOT use a different redirectTo path — same `/auth/callback?next=/role-select` as Google
    - Do NOT change the `ALLOWED_NEXT_PATHS` allowlist in auth/callback — `/role-select` is already in it
    - Do NOT hardcode Kakao client ID in code — Supabase Dashboard holds it
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('src/app/(auth)/signup/actions.ts','utf8'); if(!a.includes('signInWithKakao')) throw new Error('signInWithKakao action missing'); if(!a.includes(\"provider: 'kakao'\")) throw new Error('kakao provider string missing'); const l=fs.readFileSync('src/app/(auth)/login/page.tsx','utf8'); if(!l.includes('signInWithKakao')) throw new Error('login page missing kakao import/button'); if(!l.includes('카카오')) throw new Error('login page missing Korean Kakao label'); const s=fs.readFileSync('src/app/(auth)/signup/page.tsx','utf8'); if(!s.includes('signInWithKakao')) throw new Error('signup page missing kakao button'); console.log('OK: Kakao wired');" && npx vitest run tests/auth/kakao-oauth.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "signInWithKakao" src/app/(auth)/signup/actions.ts` == 1
    - `grep -c "provider: 'kakao'" src/app/(auth)/signup/actions.ts` == 1
    - `grep -c "signInWithKakao" src/app/(auth)/login/page.tsx` ≥ 1 (import + usage)
    - `grep -c "카카오" src/app/(auth)/login/page.tsx` ≥ 1 (Korean label)
    - `grep -c "signInWithKakao" src/app/(auth)/signup/page.tsx` ≥ 1
    - `grep -c "FEE500" src/app/(auth)/login/page.tsx` ≥ 1 (Kakao brand color)
    - Kakao action's `options.redirectTo` points to `/auth/callback?next=/role-select` (same as Google)
    - No new file created under `src/app/(auth)/` — Kakao lives in existing signup/actions.ts
    - `tests/auth/kakao-oauth.test.ts` passes (GREEN) OR is permanently skipped with `defer-kakao` note
    - `npx tsc --noEmit` clean
    - No custom OIDC implementation anywhere (grep for "OIDC" returns 0 or only documentation references)
  </acceptance_criteria>
  <done>
    Kakao action + buttons added with single provider string change. AUTH-01k test green OR deferred per user choice in Task 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full Phase 2 E2E smoke — manual verification of all 6 dev accounts + all auth flows</name>
  <what-built>
    Claude has automated:
    - Plan 01: test infra + stubs + .env.example + ARCHITECTURE drift fix
    - Plan 02: Prisma schema + Supabase migrations + PostGIS + trigger + RLS
    - Plan 03: @supabase/ssr 3-file + proxy.ts + DAL
    - Plan 04: login/signup/role-select Server Actions + OAuth callback allowlist + role-gated layouts
    - Plan 05: seed script + 6 dev accounts + mock data populated
    - Plan 06 Tasks 1-2: Kakao provider enabled + Kakao actions/buttons wired (or deferred)

    All automated tests run green. This checkpoint is the final manual smoke before Phase 2 verify-work.
  </what-built>
  <how-to-verify>
    **Prerequisites (before manual smoke):**
    ```bash
    # In one terminal
    npm run dev
    # In another terminal — re-seed to ensure fresh state
    npx prisma db seed
    # In another terminal — run full test suite
    npx vitest run && npx playwright test
    ```
    Expected: Vitest all green, Playwright tests all green (or Kakao permanently skipped if deferred).

    **Manual smoke test — perform in browser:**

    **Test A: Unauthenticated redirect (AUTH-05)**
    1. Open fresh browser incognito window
    2. Visit `http://localhost:3000/home`
    3. Expected: Redirects to `/login?next=%2Fhome`
    4. Visit `http://localhost:3000/biz/posts`
    5. Expected: Redirects to `/login?next=%2Fbiz%2Fposts`

    **Test B: Email/Password login — Worker role (AUTH-01 + AUTH-03)**
    1. Still at `/login`, enter `worker+test@gignow.dev` / `gignowdev`, click 로그인
    2. Expected: Lands on `/home` (Worker dashboard)
    3. Press F5 (hard refresh)
    4. Expected: Still on `/home` (session persisted)

    **Test C: Worker → Biz-only route blocked (AUTH-06)**
    1. While logged in as worker, manually navigate to `http://localhost:3000/biz/posts`
    2. Expected: Redirects to `/login?error=business_required`
    3. Login form should show "Business 권한이 필요합니다" (or similar Korean error message)

    **Test D: Logout (AUTH-04)**
    1. Find the logout button (may need to navigate via tab bar / profile menu)
    2. Click logout
    3. Expected: Redirects to `/login`
    4. Open DevTools → Application → Cookies → localhost:3000
    5. Expected: No cookies matching `sb-*-auth-token`

    **Test E: Business role (AUTH-07)**
    1. Login as `business+test@gignow.dev` / `gignowdev`
    2. Expected: Lands on `/biz` (Business dashboard) — NOT `/home`
    3. Navigate to `http://localhost:3000/my/profile`
    4. Expected: Redirects to `/login?error=worker_required`
    5. Logout

    **Test F: BOTH role**
    1. Login as `both+test@gignow.dev` / `gignowdev`
    2. Expected: Lands on `/home` (BOTH defaults to worker landing per Plan 04 Task 2 redirect logic)
    3. Navigate manually to `/biz` — expected: access allowed (no redirect)
    4. Navigate back to `/home` — expected: access allowed
    5. Both role cross-access confirmed
    6. Logout

    **Test G: Admin role**
    1. Login as `admin+test@gignow.dev` / `gignowdev`
    2. Expected: Lands on `/biz` (ADMIN routed per Plan 04 Task 2 redirect)
    3. Verify both `/home` and `/biz` accessible
    4. Logout

    **Test H: Signup new user (manually testing Plan 04 signup wire)**
    1. At `/login`, click 회원가입
    2. Enter a fresh email like `smoke-test@gignow.dev` + password `testtest12`
    3. Submit
    4. Expected: Redirects to `/auth/check-email` OR similar confirmation page
    5. Check Supabase Dashboard → Authentication → Users
    6. Expected: New user exists
    7. Verify `public.users` row auto-created by trigger: run `SELECT * FROM public.users WHERE email='smoke-test@gignow.dev'` via Supabase SQL editor
    8. Expected: row exists with `role = 'WORKER'` (trigger default)
    9. After smoke test: delete this user from both auth.users and public.users via Dashboard

    **Test I: Magic Link signup (optional — requires real email)**
    1. Click "Magic Link로 로그인" button
    2. Enter `worker+test@gignow.dev`
    3. Expected: success message; check Supabase Dashboard → Authentication → Logs for magic link sent entry
    4. (Skip actual email click — development SMTP typically logs to Dashboard, not real inbox, unless you configured custom SMTP)

    **Test J: Google OAuth (optional — requires Google provider toggled on)**
    1. Click "Google로 계속하기" button
    2. Expected: Redirects to Google OAuth consent screen (or to `/auth/error` if Google provider disabled — document which)
    3. If enabled: complete flow and verify lands on `/role-select`
    4. If disabled: mark as "Google OAuth smoke skipped — provider not enabled in this dev project"

    **Test K: Kakao OAuth (if not deferred in Task 1)**
    1. Click "카카오로 계속하기" button
    2. Expected: Redirects to `https://kauth.kakao.com/oauth/authorize?...`
    3. Complete with a real Kakao account
    4. Expected: Lands on `/role-select` via `/auth/callback`
    5. Verify `public.users` row created with Kakao account's email

    **Test L: Role-select flow**
    1. After Test H or K (any newly-created account without role), should be on `/role-select`
    2. Click "🙋 일하고 싶어요 (Worker)"
    3. Expected: Redirects to `/home`
    4. Verify via Supabase SQL editor: `SELECT role FROM public.users WHERE email='<new account>'` returns `WORKER`
    5. Also verify: `SELECT raw_app_meta_data->>'role' FROM auth.users WHERE email='<new account>'` returns `WORKER` (app_metadata.role set by admin API per Plan 04 Task 3)

    **Report back to Claude:**
    - Which tests PASSED, which FAILED, any error messages
    - Screenshot of DevTools cookies after Test D (confirming sb-*-auth-token gone)
    - Confirmation that 6 dev accounts all logged in successfully
    - Any visual regressions (Phase 2 should NOT change visuals — report if anything looks off)

    **If any test fails:** do NOT proceed to Task 4. Report the failure and we'll debug. If failure is due to Plan 01-05 bug, we'll create a gap closure plan.
  </how-to-verify>
  <resume-signal>Reply `smoke-green` (all pass), or list which tests failed and any error details.</resume-signal>

  <files>(checkpoint — human performs browser-based smoke test; no local file writes until Task 4)</files>
  <action>CHECKPOINT: Blocking human-verify checkpoint. Executor must halt and present the what-built and how-to-verify XML blocks above to the user. The user performs the 12-test manual smoke sequence (Tests A through L) in a browser against local next dev. Executor resumes only after user replies smoke-green or reports test failures.</action>
  <verify>
    <automated>echo 'checkpoint: manual browser smoke test required — no automated verify'</automated>
  </verify>
  <done>User replied smoke-green (all 12 manual tests passed for all 6 dev accounts) OR reported test failures that were addressed via gap closure plan before resumption.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Record AUTH-01 drift note in STATE.md + final ARCHITECTURE.md drift review + commit Phase 2</name>
  <files>.planning/STATE.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - .planning/STATE.md (current Known Risks section)
    - .planning/codebase/ARCHITECTURE.md (already modified in Plan 01 Task 4 — verify Phase 2 end state is accurate)
    - 02-RESEARCH.md lines 89-92 (drift note for AUTH-01 — "email-only per D-01")
    - 02-CONTEXT.md §domain boundary (lines 10-27 — Phase 2 scope confirmation)
  </read_first>
  <action>
    **Step 1 — Add AUTH-01 drift note to STATE.md Known Risks section.** Open `.planning/STATE.md` and find the `### Known Risks` section. Append (do NOT remove existing entries):
    ```markdown
    - **AUTH-01 drift:** REQUIREMENTS.md defines AUTH-01 as "휴대폰 번호 또는 이메일로 회원가입". Phase 2 실구현은 CONTEXT.md D-01 locked decision에 따라 **이메일 전용** (Email/Password + Magic Link + Google + Kakao OAuth). SMS OTP는 v2 (Twilio/Vonage 국내 통신사 연동 비용 + 스팸 필터 이슈). REQUIREMENTS.md는 Phase 2 완료 시점에 AUTH-01을 "이메일 인증" 기준으로 부분 충족으로 간주. v2 추가 여부는 Phase 5 이후 재평가.
    ```

    Also update the `## Current Position` section:
    - Change `**Status:** Not started` → `**Status:** Completed 2026-04-10 (or the actual completion date)`
    - Change progress bar `[#----]` → `[##---]` (2/5 phases complete)

    And the `## Phase Progress` table:
    - Change `| 2. Supabase·Prisma·Auth 기반 | **Current — Not started** | - |` to `| 2. Supabase·Prisma·Auth 기반 | Completed | YYYY-MM-DD |`
    - Change `| 3. 프로필·공고 DB 연결 | Pending | - |` to `| 3. 프로필·공고 DB 연결 | **Current — Not started** | - |`

    And the `### Next Session Starting Point` section:
    - Replace with:
      ```
      1. `cat .planning/ROADMAP.md`로 Phase 3 범위 확인
      2. `/gsd-plan-phase 3` 실행하여 Phase 3 실행 계획 수립
      3. 계획 승인 후 `/gsd-execute-phase 3`으로 Phase 3 착수
      ```

    **Step 2 — Final ARCHITECTURE.md drift review.** Plan 01 Task 4 did the initial fix (Clerk/Toss/push removal). Now verify the end state is accurate for Phase 2 completion. Open `.planning/codebase/ARCHITECTURE.md` and:
    1. Update any remaining "Planned" language to "Phase 2 complete" for auth/data-layer sections
    2. Replace aspirational "Clerk webhook at /api/webhooks/clerk" (if any fragment remains) with actual Supabase Auth + proxy.ts description
    3. Update "Authentication Strategy" section to reflect actual implementation:
       - Provider: Supabase Auth (Email/Password + Magic Link + Google OAuth + Kakao OAuth — Phase 2 complete)
       - Session Management: `@supabase/ssr` 3-file pattern + Next 16 `src/proxy.ts` with `updateSession` helper
       - Route Protection: Proxy-level optimistic check (JWT app_metadata.role) + DAL re-verification in page components
       - Multi-role Support: WORKER / BUSINESS / BOTH / ADMIN — role stored in `public.users.role` AND `auth.users.app_metadata.role`
    4. Update "Database & ORM" references to mention hybrid migration strategy (Prisma SSOT + Supabase SQL for RLS/PostGIS/triggers)
    5. Mark "AI Matching", "Toss Payments", "Push Notifications" as "v2 — not in Phase 1-5 scope"
    6. Update the "Drift note" banner at the top (added in Plan 01 Task 4) to include: "Updated 2026-04-10 Phase 2 completion: auth + database layers now reflect actual implementation."

    **Step 3 — Commit all Phase 2 changes.** Run:
    ```
    git status
    ```
    Expected: modifications to STATE.md, ARCHITECTURE.md, and any Phase 2 file changes not yet committed.

    Stage and commit (the planner agent does not run commits — the executor does this step):
    ```
    git add .planning/STATE.md .planning/codebase/ARCHITECTURE.md .planning/phases/02-supabase-prisma-auth/
    git commit -m "docs(phase-02): close Phase 2 with auth + data layers complete"
    ```

    If there are still un-staged Phase 2 source file changes (src/, prisma/, tests/, supabase/migrations/) they should already have been committed at the end of their respective Plans 01-05. Task 4 only handles the docs updates.

    **Anti-pattern guard:**
    - Do NOT mark DATA-05 as complete — it's a Phase 5 exit criterion (`src/lib/mock-data.ts` still exists per Phase 2 boundary)
    - Do NOT mark Phase 3 as Current before Phase 2 is actually committed
    - Do NOT remove the "Drift note" banner from ARCHITECTURE.md — keep it as ongoing documentation of the alignment history
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/STATE.md','utf8'); if(!s.includes('AUTH-01 drift')) throw new Error('STATE.md missing AUTH-01 drift note'); if(!s.includes('이메일 전용')) throw new Error('STATE.md drift note missing email-only mention'); if(!s.match(/Phase 2.*Completed/i)&&!s.match(/2\\. Supabase.*Completed/)) console.warn('WARNING: STATE.md Phase 2 status not updated to Completed — manual check'); const a=fs.readFileSync('.planning/codebase/ARCHITECTURE.md','utf8'); if(a.match(/Clerk \\(webhook integration/)) throw new Error('ARCHITECTURE.md still references Clerk webhook'); if(!a.includes('Supabase Auth')) throw new Error('ARCHITECTURE.md missing Supabase Auth'); if(!a.includes('@supabase/ssr')) console.warn('WARNING: ARCHITECTURE.md does not mention @supabase/ssr — may need final update'); if(!a.includes('Drift note')) throw new Error('ARCHITECTURE.md missing drift note banner'); console.log('OK: STATE drift recorded + ARCHITECTURE final review');" && npx vitest run 2>&1 | tail -10</automated>
    <manual_phase_close>Phase close gate (NOT sampled automatically — exempt from the 30s Nyquist latency target per VALIDATION.md because it is the final phase-close verification): run `npx playwright test tests/e2e/` manually. Expect all 5 E2E specs (session-persist, logout, protected-redirect, role-worker-only, role-biz-only) to pass. If any fail, the phase is NOT complete — create a gap closure plan. This step MUST execute successfully before Phase 2 is declared closed, but the command lives here (not in the automated verify block) because a full Playwright run exceeds the 30s feedback-latency budget for automated verification.</manual_phase_close>
  </verify>
  <acceptance_criteria>
    - `grep -c "AUTH-01 drift" .planning/STATE.md` == 1
    - `grep -c "이메일 전용" .planning/STATE.md` == 1
    - `grep -c "SMS OTP" .planning/STATE.md` ≥ 1 (drift note references SMS v2)
    - STATE.md Phase Progress table shows Phase 2 as Completed
    - STATE.md Current Position shows Phase 3 (or later) as next
    - `grep -c "Clerk (webhook integration" .planning/codebase/ARCHITECTURE.md` == 0
    - `grep -c "@supabase/ssr" .planning/codebase/ARCHITECTURE.md` ≥ 1
    - `grep -c "Drift note" .planning/codebase/ARCHITECTURE.md` ≥ 1
    - `grep -c "src/proxy.ts\\|proxy.ts" .planning/codebase/ARCHITECTURE.md` ≥ 1 (mentions Next 16 proxy pattern)
    - Automated sampling: `npx vitest run` exits 0 with all non-skipped tests green
    - Manual phase-close gate: operator has run `npx playwright test tests/e2e/` at least once and recorded the result in 02-06-SUMMARY.md; all 5 E2E specs green (or Kakao-related spec skipped if Kakao was deferred in Task 1). This step is exempt from the automated 30s Nyquist latency target because it is the final phase-close verification, not a continuous feedback loop.
    - `src/lib/mock-data.ts` still exists (Phase 5 exit criterion preserved)
    - ALL 11 REQ-IDs (DATA-01..04, AUTH-01..07) have a green test in VALIDATION.md status column
  </acceptance_criteria>
  <done>
    STATE.md updated with AUTH-01 drift, ARCHITECTURE.md final state matches actual implementation, Vitest green via automated sampling, **E2E suite green — verified via manual Playwright run (`npx playwright test tests/e2e/`), not automated sampling**. Phase 2 ready for `/gsd-verify-work 2`.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kakao Developers → Supabase | Redirect URI must be Supabase's, not the app's (Pitfall #9) |
| Supabase Dashboard → Kakao secret | Secret stored server-side at Supabase, never in client bundle |
| Manual smoke testing | Human-in-the-loop verification — not automated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Kakao redirect URI mismatch enables attacker to redirect auth to their own endpoint | mitigate | Task 1 manual instructions require setting redirect URI to `https://<project-ref>.supabase.co/auth/v1/callback` — Supabase's endpoint, not the app's. Pitfall #9 documented in anti_patterns. |
| T-06-02 | Spoofing | Kakao client secret leaked via version control | mitigate | Secret pasted directly into Supabase Dashboard, never into app code or `.env.local`. `.env.example` has placeholder only (from Plan 01). |
| T-06-03 | Elevation | Kakao OAuth bypasses role-select flow | mitigate | `signInWithKakao` uses same `redirectTo: /auth/callback?next=/role-select` as Google. Plan 04 Task 2 confirmed the allowlist includes `/role-select`. New users land on role-select and must choose explicitly before reaching role-gated routes. |
| T-06-04 | Tampering | OAuth open redirect (inherited from Plan 04) | mitigate | Inherited mitigation: `ALLOWED_NEXT_PATHS` allowlist in `/auth/callback/route.ts` applies to Kakao equally. |
| T-06-05 | Info disclosure | STATE.md drift note leaks internal decision rationale | accept | Drift note is intentional documentation for future phase planners. No secrets or PII leaked. |
| T-06-06 | Denial of service | Smoke test accidentally creates production accounts | accept | Smoke test runs against local `next dev` + dev Supabase project. Production guard in seed (Plan 05) + manual test user cleanup in Task 3 Test H. |
| T-06-07 | Elevation | Kakao user account inherits higher privileges than intended | mitigate | Trigger `handle_new_user` always defaults role to WORKER on insert. Role can only be elevated via role-select action which calls `verifySession` before admin API call. |

**Applicable ASVS L1 categories:** V2 Authentication (Kakao provider PKCE flow), V14 Configuration (Supabase Dashboard vs env vars separation).

</threat_model>

<verification>
- Task 1 checkpoint resolved (kakao-ready or defer-kakao)
- `signInWithKakao` action present OR test marked permanently skipped
- Kakao buttons on login + signup pages (if not deferred)
- Task 3 manual smoke checkpoint passed for all 6 dev accounts
- STATE.md drift note present
- ARCHITECTURE.md drift final-state updated
- Full test suite GREEN (vitest + playwright)
- All 11 REQ-IDs in VALIDATION.md status column GREEN
</verification>

<success_criteria>
Plan 06 is successful when:
1. Kakao provider enabled in Supabase Dashboard + Kakao Developers app configured (or user deferred)
2. `signInWithKakao` Server Action added to signup/actions.ts with `provider: 'kakao'` one-liner
3. Kakao buttons present on login and signup pages
4. AUTH-01k test GREEN (or permanently skipped with defer note)
5. Manual smoke test passed for all 6 dev accounts (Test A through L)
6. AUTH-01 drift note recorded in STATE.md Known Risks
7. ARCHITECTURE.md reflects Phase 2 actual implementation (Clerk/Toss/push removed, Supabase Auth + proxy.ts + DAL documented)
8. Full Vitest + Playwright suite GREEN
9. STATE.md Phase Progress shows Phase 2 Completed, Phase 3 as next Current
10. Phase 2 is ready for `/gsd-verify-work 2` — all 11 REQ-IDs (DATA-01..04, AUTH-01..07) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-prisma-auth/02-06-SUMMARY.md` with:
- Kakao status: enabled / deferred
- Manual smoke test results for Tests A-L (pass/fail per test)
- Final `npx vitest run` exit code + test count
- Final `npx playwright test` exit code + test count
- List of all REQ-IDs with their final status (all expected GREEN)
- Confirmation that `src/lib/mock-data.ts` is still present and unmodified (Phase 5 exit criterion preservation)
- Confirmation that STATE.md drift note added
- Any follow-ups for Phase 3 planner (e.g., "role-select JWT claim timing issue — document")
- Phase 2 total effort metric: number of tasks completed across 6 plans
</output>

---
phase: 02-supabase-prisma-auth
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/proxy.ts
  - src/lib/dal.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-05
  - AUTH-06
  - AUTH-07
tags: [supabase, ssr, proxy, session, dal, middleware]

must_haves:
  truths:
    - "@supabase/ssr의 3-file 패턴 (client.ts, server.ts, middleware.ts helper)이 RESEARCH.md verbatim 코드로 존재한다"
    - "`src/proxy.ts` (NOT middleware.ts)가 Next 16 proxy 파일 규약으로 존재한다"
    - "proxy가 `/home`, `/my`, `/biz` 등 보호 경로에 대해 비로그인 시 `/login?next=...` 리다이렉트를 수행한다"
    - "proxy가 Worker 전용 경로에 BUSINESS 사용자를 차단하고, Business 전용 경로에 WORKER 사용자를 차단한다 (optimistic check via JWT app_metadata.role)"
    - "`src/lib/dal.ts`가 `verifySession`, `requireWorker`, `requireBusiness`를 `react.cache`로 래핑해 export한다"
    - "DAL 헬퍼는 `server-only` import를 통해 클라이언트 번들에 절대 포함되지 않는다"
    - "모든 server code는 `getSession()`을 사용하지 않는다 (getClaims in proxy, getUser in pages)"
    - "tests/proxy/redirect.test.ts가 녹색으로 통과한다 (AUTH-05 unit)"
  artifacts:
    - path: src/lib/supabase/client.ts
      provides: "Browser Supabase client (createBrowserClient)"
      contains: "createBrowserClient"
    - path: src/lib/supabase/server.ts
      provides: "Server Component/Action Supabase client (createServerClient with getAll/setAll)"
      contains: "createServerClient"
    - path: src/lib/supabase/middleware.ts
      provides: "updateSession helper for proxy.ts"
      exports: ["updateSession"]
    - path: src/proxy.ts
      provides: "Next 16 proxy file (matcher + session refresh + role check)"
      contains: "export async function proxy"
    - path: src/lib/dal.ts
      provides: "DAL: verifySession / requireWorker / requireBusiness"
      exports: ["verifySession", "requireWorker", "requireBusiness"]
      contains: "import 'server-only'"
  key_links:
    - from: "src/proxy.ts"
      to: "src/lib/supabase/middleware.ts updateSession"
      via: "ESM import"
      pattern: "from ['\"]@/lib/supabase/middleware['\"]"
    - from: "src/lib/supabase/middleware.ts"
      to: "supabase.auth.getClaims()"
      via: "direct SDK call"
      pattern: "getClaims\\(\\)"
    - from: "src/lib/dal.ts"
      to: "prisma.user.findUnique"
      via: "DB fetch for authoritative role"
      pattern: "prisma\\.user\\.findUnique"
---

<objective>
`@supabase/ssr@0.10.2` 3-file 패턴을 Next 16 `proxy.ts` 규약으로 얹고, Next 16 data-security 가이드가 요구하는 `src/lib/dal.ts` re-verification layer를 만든다.

**목적:** Plan 04의 모든 auth Server Action은 이 Plan이 제공하는 `createClient` (server.ts)와 `verifySession` (dal.ts)에 의존한다. Plan 04의 role-gated layouts는 `requireWorker`/`requireBusiness`로 second-layer 검증을 한다. Plan 06의 Kakao wave는 같은 OAuth helper 위에 button만 추가한다. 즉 Plan 03은 Phase 2의 auth 기반 클래스다.

**출력:** (1) `src/lib/supabase/{client,server,middleware}.ts` — 모두 RESEARCH.md verbatim, (2) `src/proxy.ts` (NOT middleware.ts — Next 16 규약), (3) `src/lib/dal.ts` with `'server-only'` import + `react.cache`, (4) 녹색이 된 `tests/proxy/redirect.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-supabase-prisma-auth/02-CONTEXT.md
@.planning/phases/02-supabase-prisma-auth/02-RESEARCH.md
@.planning/phases/02-supabase-prisma-auth/02-VALIDATION.md
@.planning/phases/02-supabase-prisma-auth/02-01-SUMMARY.md
@.planning/phases/02-supabase-prisma-auth/02-02-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@src/lib/db/index.ts
@tests/proxy/redirect.test.ts

<interfaces>
Contracts this plan creates (Plan 04 depends on these):

- `src/lib/supabase/client.ts` exports `createClient(): SupabaseClient` (createBrowserClient wrapper)
- `src/lib/supabase/server.ts` exports `async createClient(): Promise<SupabaseClient>` (createServerClient, awaits cookies())
- `src/lib/supabase/middleware.ts` exports `async updateSession(request: NextRequest): Promise<NextResponse>`
- `src/proxy.ts` exports `async proxy(request)` + `config.matcher` array
- `src/lib/dal.ts` exports:
  - `verifySession: () => Promise<{ id: string; email: string; role: UserRole }>`
  - `requireWorker: () => Promise<{ id: string; email: string; role: UserRole }>`
  - `requireBusiness: () => Promise<{ id: string; email: string; role: UserRole }>`

Redirect URL shapes (used by Plan 04 and tests):
- Unauth → `/login?next=<original-path>`
- Worker route + non-worker user → `/login?error=worker_required`
- Biz route + non-biz user → `/login?error=business_required`

Path prefix sets (consumed by proxy helper and DAL):
- Worker: `['/home','/my','/explore','/search','/notifications','/apply','/chat','/posts']` (matches actual children of `src/app/(worker)/`)
- Business: `['/biz']` (matches actual flat `src/app/biz/` routes including `/biz/posts`)
- Public: `['/','/login','/signup','/auth']`
</interfaces>

<anti_patterns>
- **NEVER** create `src/middleware.ts`. Next 16 requires `src/proxy.ts` (Pitfall #1 in RESEARCH.md:L1297).
- **NEVER** use `get`/`set`/`remove` cookie callbacks. `@supabase/ssr@0.10.2` requires `getAll`/`setAll` ONLY (RESEARCH.md Key Finding #3 + Pitfall L619).
- **NEVER** run DB queries between `createServerClient()` and `supabase.auth.getClaims()` — causes random logouts (RESEARCH.md Pitfall #3).
- **NEVER** use `supabase.auth.getSession()` in server code — `getClaims()` in proxy, `getUser()` in pages/actions (Pitfall #8).
- **NEVER** check auth in a Layout — Partial Rendering means it won't re-run on client nav (Pitfall #7).
- **NEVER** store the Supabase client in a module-level global — Fluid Compute requires per-request instance.
- **NEVER** import `@/lib/supabase/server` in a `'use client'` file.
- **NEVER** import `@/lib/dal` in client code — `'server-only'` will throw at build.
- Do NOT install Auth.js / NextAuth / Clerk — locked to `@supabase/ssr` per CLAUDE.md.
</anti_patterns>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write @supabase/ssr 3-file pattern (client.ts / server.ts / middleware.ts helper) verbatim from RESEARCH.md</name>
  <files>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/middleware.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 194-347 (Pattern 1 — full verbatim 3-file code block)
    - 02-RESEARCH.md lines 280-287 (critical "Do not run code between createServerClient and getClaims" comment — preserve verbatim)
    - 02-RESEARCH.md lines 289-345 (GigNow-specific role-based protection block)
    - 02-RESEARCH.md lines 614-626 (Anti-Patterns — all 9)
    - 02-CONTEXT.md lines 157-173 (D-06 — Next 16 proxy pattern)
    - AGENTS.md (Next 16 breaking changes reminder)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (MANDATORY — authoritative Next 16 proxy API)
  </read_first>
  <action>
    Create three files. **All three must be copied VERBATIM** from 02-RESEARCH.md with ONLY the `@/` path alias applied. Do NOT re-derive from training data — the RESEARCH.md blocks are the canonical source (fetched 2026-04-10 from official Supabase/Vercel examples).

    **`src/lib/supabase/client.ts`** — copy from 02-RESEARCH.md:L199-L208:
    ```ts
    import { createBrowserClient } from '@supabase/ssr'

    export function createClient() {
      return createBrowserClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
      )
    }
    ```

    Env var: the primary name is `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` per RESEARCH.md Key Finding #5. If `.env.local` still uses `NEXT_PUBLIC_SUPABASE_ANON_KEY`, add the fallback:
    ```ts
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY ?? process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
    ```
    and leave an inline comment referencing CONTEXT.md D-07.

    **`src/lib/supabase/server.ts`** — copy from 02-RESEARCH.md:L212-L241:
    ```ts
    import { createServerClient } from '@supabase/ssr'
    import { cookies } from 'next/headers'

    export async function createClient() {
      const cookieStore = await cookies()

      return createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
        {
          cookies: {
            getAll() {
              return cookieStore.getAll()
            },
            setAll(cookiesToSet, _headers) {
              try {
                cookiesToSet.forEach(({ name, value, options }) =>
                  cookieStore.set(name, value, options)
                )
              } catch {
                // The setAll method was called from a Server Component.
                // This can be ignored if you have middleware refreshing
                // user sessions.
              }
            },
          },
        }
      )
    }
    ```

    `cookies()` MUST be awaited (Next 15+ async API — RESEARCH.md §State of the Art L1529). The try/catch on setAll is intentional per Pitfall #4 — do NOT remove.

    **`src/lib/supabase/middleware.ts`** — copy from 02-RESEARCH.md:L245-L347. This file is the most critical (~100 lines) and contains:
    1. `createServerClient` with request-level getAll/setAll (NOT cookieStore-level)
    2. The verbatim comment block "Do not run code between createServerClient and supabase.auth.getClaims()" — preserve exactly
    3. `const { data } = await supabase.auth.getClaims()` (NOT getUser, NOT getSession)
    4. GigNow-specific role-based protection (RESEARCH.md:L289-L345): workerPrefixes array, bizPrefixes array, path matching, redirect with `error=worker_required` / `error=business_required`
    5. `searchParams.set('next', path)` for unauth redirect
    6. `return supabaseResponse` at every happy path (never a fresh `NextResponse.next()` — would drop cookie updates)

    Invariants that must survive the copy:
    - `let supabaseResponse = NextResponse.next({ request })` — `let` not `const` (reassigned inside setAll)
    - ZERO statements between `createServerClient(...)` and `await supabase.auth.getClaims()` — no logging, no DB, no pre-read of path
    - `const role = user?.app_metadata?.role as 'WORKER' | 'BUSINESS' | 'BOTH' | 'ADMIN' | undefined`
    - `workerPrefixes` MUST match the actual children of `src/app/(worker)/` — verified 2026-04-10 via `ls src/app/(worker)`. The correct list is exactly 8 paths: `/home, /my, /explore, /search, /notifications, /apply, /chat, /posts`. Do NOT include `/schedule`, `/settlements`, `/applications`, or `/availability` — these do NOT exist under `(worker)/` and were a drift error in earlier plan drafts. Note that `/posts` IS a worker route (the worker-facing job feed); the business-side posts UI lives at `/biz/posts` which is covered by `bizPrefixes`.
    - `bizPrefixes = ['/biz']` (catches `/biz`, `/biz/posts`, `/biz/anything` via startsWith)
    - Public exclusion: `path.startsWith('/login') || path.startsWith('/signup') || path.startsWith('/auth') || path === '/'`

    **Anti-pattern guard:**
    - Do NOT change `getClaims()` to `getUser()` or `getSession()`
    - Do NOT add any code between createServerClient and getClaims (even a comment that triggers re-ordering)
    - Do NOT refactor setAll to DRY — the verbatim pattern is subtle (request.cookies.set inside loop, then reassign supabaseResponse, then supabaseResponse.cookies.set)
    - Do NOT add `'use client'` directive anywhere
    - Do NOT rename the middleware.ts file — the helper IS named middleware.ts, only the ROOT `src/proxy.ts` uses the new Next 16 name (it imports from `@/lib/supabase/middleware`)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/lib/supabase/client.ts','src/lib/supabase/server.ts','src/lib/supabase/middleware.ts']; for(const f of files){if(!fs.existsSync(f)) throw new Error('Missing: '+f);} const client=fs.readFileSync(files[0],'utf8'); if(!client.includes('createBrowserClient')) throw new Error('client.ts missing createBrowserClient'); const server=fs.readFileSync(files[1],'utf8'); if(!server.includes('createServerClient')) throw new Error('server.ts missing createServerClient'); if(!server.includes('await cookies()')) throw new Error('server.ts missing await cookies()'); if(!server.includes('getAll()')||!server.includes('setAll(')) throw new Error('server.ts missing getAll/setAll'); const mw=fs.readFileSync(files[2],'utf8'); if(!mw.includes('getClaims()')) throw new Error('middleware.ts missing getClaims()'); if(mw.includes('getSession')) throw new Error('middleware.ts uses deprecated getSession'); if(!mw.includes('workerPrefixes')||!mw.includes('bizPrefixes')) throw new Error('middleware.ts missing role prefix protection'); if(!mw.includes('worker_required')||!mw.includes('business_required')) throw new Error('middleware.ts missing role error signals'); console.log('OK: 3-file pattern verbatim, getClaims + role check present');" && npx tsc --noEmit 2>&1 | (grep -E "supabase/(client|server|middleware)\.ts" || echo "OK: no type errors in supabase helpers")</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/supabase/client.ts` exists; `grep -c "createBrowserClient" src/lib/supabase/client.ts` == 1
    - `src/lib/supabase/client.ts` does NOT contain `'use client'`
    - `src/lib/supabase/server.ts` exists; `grep -c "createServerClient" src/lib/supabase/server.ts` == 1
    - `src/lib/supabase/server.ts` contains `await cookies()`
    - `src/lib/supabase/server.ts` contains both `getAll()` and `setAll(`
    - `src/lib/supabase/middleware.ts` contains `getClaims()` exactly once
    - `src/lib/supabase/middleware.ts` does NOT contain `getSession` (Pitfall #8)
    - `src/lib/supabase/middleware.ts` contains both `workerPrefixes` and `bizPrefixes`
    - `workerPrefixes` array contains exactly these 8 paths (grep each): `/home`, `/my`, `/explore`, `/search`, `/notifications`, `/apply`, `/chat`, `/posts`
    - `workerPrefixes` array does NOT contain any of: `/schedule`, `/settlements`, `/applications`, `/availability` (these directories do not exist; their inclusion would be a drift error)
    - `src/lib/supabase/middleware.ts` contains both `error=worker_required` and `error=business_required`
    - `src/lib/supabase/middleware.ts` contains `searchParams.set('next'` for login redirect preservation
    - `npx tsc --noEmit` produces no errors for the three files
    - `ls src/middleware.ts` returns no such file
  </acceptance_criteria>
  <done>
    Three Supabase helpers written verbatim, no anti-patterns, TypeScript clean, ready for Task 2 to wire `updateSession` into the root proxy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/proxy.ts root file per Next 16 convention and turn tests/proxy/redirect.test.ts green</name>
  <files>src/proxy.ts</files>
  <behavior>
    - `src/proxy.ts` exists at `src/` root (NOT `middleware.ts`)
    - The proxy matcher matches `/home`, `/my`, `/biz/posts`, excludes `/_next/static`, `/_next/image`, `/favicon.ico`, and common image extensions
    - `tests/proxy/redirect.test.ts` (Plan 01 Task 3 stub) turns GREEN or skips with a documented experimental-API note
    - `next dev` starts without module resolution errors
  </behavior>
  <read_first>
    - 02-RESEARCH.md lines 349-370 (Pattern 1 `src/proxy.ts` — verbatim code block)
    - 02-RESEARCH.md lines 1297-1302 (Pitfall #1 — middleware.ts vs proxy.ts)
    - 02-RESEARCH.md lines 1463-1465 (Next 16 experimental testing helper `unstable_doesProxyMatch`)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (MANDATORY)
    - tests/proxy/redirect.test.ts (Plan 01 stub — this task makes it green)
    - src/lib/supabase/middleware.ts (must export `updateSession`)
  </read_first>
  <action>
    Create `src/proxy.ts` verbatim from 02-RESEARCH.md:L350-L370:
    ```ts
    import { type NextRequest } from 'next/server'
    import { updateSession } from '@/lib/supabase/middleware'

    export async function proxy(request: NextRequest) {
      return await updateSession(request)
    }

    export const config = {
      matcher: [
        /*
         * Match all request paths except for:
         * - _next/static (static files)
         * - _next/image (image optimization files)
         * - favicon.ico
         * - any image file extension
         */
        '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
      ],
    }
    ```

    **Path note:** The file MUST be at `src/proxy.ts` (root of `src/`). Next 16's proxy convention places it exactly where `middleware.ts` used to live. Verify by reading `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` BEFORE writing.

    **After writing**, run the stub from Plan 01 Task 3:
    ```
    npx vitest run tests/proxy/redirect.test.ts --reporter=dot
    ```
    If the experimental `unstable_doesProxyMatch` API works, the assertions should pass. If the API has moved/broken, the test has a documented fallback (`it.todo`) per Plan 01 Task 3 action. Acceptance criteria accepts either GREEN or TODO-skipped, but must NOT accept an unrelated runtime error.

    **Anti-pattern guard:**
    - Do NOT create `src/middleware.ts`
    - Do NOT create `middleware.ts` at project root
    - Do NOT duplicate role prefix logic in proxy.ts — it lives in `updateSession` helper
    - Do NOT add `runtime: 'edge'` — Next 16 defaults proxy to Node.js (per C-10 in RESEARCH.md:L70), Fluid Compute compatible
    - Do NOT add additional matcher entries without consulting Next 16 proxy.md docs
  </action>
  <verify>
    <automated>test -f src/proxy.ts && ! test -f src/middleware.ts && ! test -f middleware.ts && node -e "const fs=require('fs'); const p=fs.readFileSync('src/proxy.ts','utf8'); if(!p.includes('updateSession')) throw new Error('proxy.ts missing updateSession import'); if(!p.includes('@/lib/supabase/middleware')) throw new Error('proxy.ts missing helper path'); if(!p.includes('export async function proxy')) throw new Error('proxy.ts missing proxy export'); if(!p.includes('matcher')) throw new Error('proxy.ts missing matcher config'); if(!p.includes('_next/static')) throw new Error('proxy.ts matcher missing static exclusion'); if(p.includes(\"runtime: 'edge'\")) throw new Error('proxy.ts should not force edge runtime'); console.log('OK: proxy.ts valid Next 16 shape');" && npx vitest run tests/proxy/redirect.test.ts --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` exists
    - `src/middleware.ts` does NOT exist
    - `middleware.ts` at project root does NOT exist
    - `grep -c "export async function proxy" src/proxy.ts` == 1
    - `grep -c "export const config" src/proxy.ts` == 1
    - `grep -c "updateSession" src/proxy.ts` == 1
    - `grep -c "@/lib/supabase/middleware" src/proxy.ts` == 1
    - `grep -c "_next/static" src/proxy.ts` == 1
    - `grep -c "favicon.ico" src/proxy.ts` == 1
    - `grep -c "runtime:" src/proxy.ts` == 0 (no edge/node override — default is correct)
    - `npx vitest run tests/proxy/redirect.test.ts` exits 0 (green OR skipped with documented experimental-API fallback)
    - `npx tsc --noEmit` clean on src/proxy.ts
  </acceptance_criteria>
  <done>
    Next 16 proxy file at correct path, matcher excludes static assets, updateSession wired, AUTH-05 unit test condition met.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/lib/dal.ts (verifySession / requireWorker / requireBusiness) with 'server-only' + react.cache</name>
  <files>src/lib/dal.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 568-612 (Pattern 7 — full verbatim DAL code block)
    - 02-RESEARCH.md lines 1506-1516 (Next 16 Data Security Guide — MUST-FOLLOW per-action re-verification)
    - 02-RESEARCH.md lines 1338-1344 (Pitfall #7 — Layout auth check gives false security)
    - node_modules/next/dist/docs/01-app/02-guides/authentication.md (MANDATORY — DAL section, cited in RESEARCH.md:L612)
    - node_modules/next/dist/docs/01-app/02-guides/data-security.md (MANDATORY — "re-verify inside the action" rule)
    - src/lib/db/index.ts (prisma client import path)
    - src/lib/supabase/server.ts (Task 1 — createClient import target)
  </read_first>
  <action>
    Create `src/lib/dal.ts` verbatim from 02-RESEARCH.md:L572-L610:
    ```ts
    import 'server-only'
    import { cache } from 'react'
    import { createClient } from '@/lib/supabase/server'
    import { prisma } from '@/lib/db'
    import { redirect } from 'next/navigation'

    export const verifySession = cache(async () => {
      const supabase = await createClient()
      const { data: { user }, error } = await supabase.auth.getUser()
      if (error || !user) redirect('/login')

      // Fetch DB row for role (JWT claim may be stale right after role-select)
      const dbUser = await prisma.user.findUnique({
        where: { id: user.id },
        select: { id: true, email: true, role: true },
      })
      if (!dbUser) redirect('/login?error=user_not_found')

      return { id: dbUser.id, email: dbUser.email, role: dbUser.role }
    })

    export const requireWorker = cache(async () => {
      const session = await verifySession()
      if (session.role !== 'WORKER' && session.role !== 'BOTH' && session.role !== 'ADMIN') {
        redirect('/login?error=worker_required')
      }
      return session
    })

    export const requireBusiness = cache(async () => {
      const session = await verifySession()
      if (session.role !== 'BUSINESS' && session.role !== 'BOTH' && session.role !== 'ADMIN') {
        redirect('/login?error=business_required')
      }
      return session
    })
    ```

    **Key invariants (must not be altered):**
    - `import 'server-only'` at the top — enforces client bundle exclusion
    - `import { cache } from 'react'` — per-request React cache (NOT `next/cache`, NOT `unstable_cache`)
    - `getUser()` not `getSession()` — Pitfall #8
    - `prisma.user.findUnique` reads DB for authoritative role (JWT may lag by up to one session refresh — RESEARCH.md:L586)
    - Both `requireWorker` and `requireBusiness` permit `BOTH` and `ADMIN` roles
    - Redirect URLs use `error=worker_required` / `error=business_required` (Plan 04 login page displays these)
    - Both helpers wrapped in `cache()` so multiple calls in one render dedupe

    **Anti-pattern guard:**
    - Do NOT change `'server-only'` to a comment or remove it
    - Do NOT use `getClaims()` in DAL (that's proxy context; DAL runs in page/action where `getUser()` is correct per RESEARCH.md:L612)
    - Do NOT skip the `prisma.user.findUnique` and rely on JWT claims alone
    - Do NOT accept `EMPLOYER` role (renamed to BUSINESS in Plan 02)
    - Do NOT add role hierarchy ("admin impersonates worker") — Phase 2 is flat role gating
    - Do NOT add a layout-level wrapper for DAL (Pitfall #7 — always call from page component)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const dal=fs.readFileSync('src/lib/dal.ts','utf8'); if(!dal.includes(\"import 'server-only'\")) throw new Error('missing server-only'); if(!dal.includes(\"from 'react'\")||!dal.includes('cache')) throw new Error('missing react cache import'); if(!dal.includes('getUser()')) throw new Error('not using getUser'); if(dal.includes('getSession')) throw new Error('uses deprecated getSession'); if(!dal.includes('prisma.user.findUnique')) throw new Error('missing DB re-verify'); if(!dal.includes('verifySession')) throw new Error('missing verifySession export'); if(!dal.includes('requireWorker')) throw new Error('missing requireWorker export'); if(!dal.includes('requireBusiness')) throw new Error('missing requireBusiness export'); if(!dal.includes('worker_required')) throw new Error('missing worker_required error signal'); if(!dal.includes('business_required')) throw new Error('missing business_required error signal'); if(dal.includes('EMPLOYER')) throw new Error('legacy EMPLOYER role present'); console.log('OK: DAL invariants intact');" && npx tsc --noEmit 2>&1 | (grep -E "dal\\.ts" || echo "OK: no type errors in dal.ts")</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/dal.ts` exists
    - `grep -c "import 'server-only'" src/lib/dal.ts` == 1
    - `grep -c "from 'react'" src/lib/dal.ts` == 1 AND `grep -c "cache" src/lib/dal.ts` ≥ 3 (import + 3 usages)
    - `grep -c "getUser()" src/lib/dal.ts` == 1
    - `grep -c "getSession" src/lib/dal.ts` == 0
    - `grep -c "prisma.user.findUnique" src/lib/dal.ts` == 1
    - `grep -c "export const verifySession" src/lib/dal.ts` == 1
    - `grep -c "export const requireWorker" src/lib/dal.ts` == 1
    - `grep -c "export const requireBusiness" src/lib/dal.ts` == 1
    - `grep -c "worker_required" src/lib/dal.ts` == 1
    - `grep -c "business_required" src/lib/dal.ts` == 1
    - `grep -c "EMPLOYER" src/lib/dal.ts` == 0
    - `grep -c "BOTH" src/lib/dal.ts` ≥ 2 (both requireWorker and requireBusiness permit BOTH)
    - `grep -c "ADMIN" src/lib/dal.ts` ≥ 2
    - `npx tsc --noEmit` clean on src/lib/dal.ts
  </acceptance_criteria>
  <done>
    DAL module created, server-only + react cache + DB re-verification all in place, Plan 04 can now import `requireWorker` / `requireBusiness` from page components.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ proxy | Session cookies cross this boundary; proxy refreshes tokens without exposing service_role |
| Proxy ↔ Supabase | `getClaims()` verifies HMAC against Supabase JWKS; cookie-only reads are untrusted |
| Server Component ↔ client bundle | `'server-only'` marker on DAL prevents bundling |
| DAL → Prisma | Authoritative role read from DB, not trusted from JWT claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Forged session cookie bypasses proxy | mitigate | `getClaims()` verifies HMAC signature against Supabase JWKS (RESEARCH.md §Pattern 1 inline + Pitfall #8). Signature check rejects forged tokens. |
| T-03-02 | Tampering | Cookie race during concurrent requests causes random logout | mitigate | Verbatim `updateSession` with `let supabaseResponse` reassignment per Pitfall #3. No code between createServerClient and getClaims. Acceptance criteria enforces the comment block presence. |
| T-03-03 | Info disclosure | DAL leaks to client bundle | mitigate | `import 'server-only'` at top of `src/lib/dal.ts` — build fails if bundled to client. Acceptance criteria greps for the literal string. |
| T-03-04 | Elevation | Stale JWT app_metadata.role passes proxy but DB shows different role | mitigate | Proxy does optimistic check only; DAL re-reads from `prisma.user.findUnique` (RESEARCH.md:L585-L592). Per-action and per-page re-verification required per Next 16 data-security.md. |
| T-03-05 | Elevation | Worker user hits Business-only route, proxy check bypassed by direct server call | mitigate | Two-layer defense: (1) proxy optimistic check on `app_metadata.role`, (2) `requireBusiness()` DAL call at top of `src/app/biz/layout.tsx` / page components (Plan 04 scope). Plan 04 wires the second layer. |
| T-03-06 | Tampering | Worker prefix list out of sync with actual routes | mitigate | Prefix lists are in single file `src/lib/supabase/middleware.ts` — grep-verifiable, reviewable. Acceptance criteria enforces all 11 worker prefixes + `/biz`. |
| T-03-07 | Denial of service | getClaims() network call fails mid-request | accept | Supabase JWKS is cached; failure returns null `data.claims` → user treated as unauth → `/login` redirect. Degraded UX, no auth bypass. |
| T-03-08 | Info disclosure | Proxy matcher leaks sensitive static files | mitigate | Matcher excludes `_next/static`, `_next/image`, `favicon.ico`, common image extensions. Dynamic routes pass through proxy so all data endpoints are gated. |
| T-03-09 | Elevation | Layout-level auth check gives false security | mitigate | DAL helpers ONLY callable from page components (Plan 04 convention). Pitfall #7 documented in anti_patterns section. Executors see the warning in every Plan 04 task. |
| T-03-10 | Spoofing | Cookie theft via XSS | mitigate | Supabase SSR defaults: HttpOnly + SameSite=lax + Secure. Unchanged by our code. |

**Applicable ASVS L1:** V2 Authentication (getClaims HMAC verify), V3 Session Management (HttpOnly cookies + per-request refresh), V4 Access Control (DAL re-verify + two-layer defense), V8 Data Protection (`server-only` marker), V13 API & Web Service (Server Action Origin check handled by Next 16 built-in).

</threat_model>

<verification>
- All 5 files present at correct paths
- `src/middleware.ts` does NOT exist (Pitfall #1 enforced)
- `src/proxy.ts` matcher regex correct
- `src/lib/supabase/middleware.ts` contains the `getClaims()` call and GigNow role prefix lists
- `src/lib/dal.ts` has `'server-only'` + `react.cache` + Prisma DB re-verify
- `npx vitest run tests/proxy/redirect.test.ts` exits 0 (AUTH-05 unit condition met)
- `npx tsc --noEmit` clean on all new files
</verification>

<success_criteria>
Plan 03 is successful when:
1. `@supabase/ssr` 3-file pattern written verbatim from RESEARCH.md (no anti-pattern drift)
2. `src/proxy.ts` exists at Next 16-correct path, no `middleware.ts` present anywhere
3. Proxy matcher excludes static assets, matches all app routes
4. DAL helpers available from `@/lib/dal` with `'server-only'` + `react.cache` + DB re-verification
5. AUTH-05 unit test (`tests/proxy/redirect.test.ts`) turns GREEN
6. Plan 04 can now import `createClient` (server.ts) and `requireWorker`/`requireBusiness` (dal.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-prisma-auth/02-03-SUMMARY.md` with:
- File list with byte sizes (confirms not empty stubs)
- TypeScript compile status (`npx tsc --noEmit` exit code)
- Vitest result for `tests/proxy/redirect.test.ts` (green / todo-skipped / failed)
- Note if `NEXT_PUBLIC_SUPABASE_ANON_KEY` fallback was needed in client.ts (CONTEXT.md D-07 drift)
- Confirmation that `src/middleware.ts` does not exist
</output>

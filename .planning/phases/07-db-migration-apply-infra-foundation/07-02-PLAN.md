---
phase: 07-db-migration-apply-infra-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - supabase/migrations/20260414000005_phase6_admin_seed.sql
  - .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md
  - src/generated/prisma/
autonomous: false
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04
user_setup:
  - service: supabase
    why: "Phase 6 마이그레이션 적용 대상 실 Supabase 프로젝트 접근 + 승격할 dev 계정 이메일 확인 + 로그아웃/재로그인 수동 절차"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection string (pooler)"
      - name: DIRECT_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection string (direct)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role (masked in all outputs)"
    dashboard_config:
      - task: "승격할 dev 계정이 auth.users에 이미 존재하는지 Supabase Dashboard → Authentication → Users에서 확인"
        location: "Supabase Dashboard → Authentication → Users"
      - task: "signed URL RLS 왕복 검증을 위해 Business 계정으로 /biz/verify 이미지 업로드 수행"
        location: "local Next.js dev server + Supabase Storage Dashboard"
tags:
  - migration
  - supabase
  - prisma
  - admin-seed
  - storage-rls

must_haves:
  truths:
    - "`npx prisma validate` + `npx prisma migrate status` + `npx prisma generate`가 drift/pending 0건으로 clean하게 종료된다"
    - "승격된 Admin 계정이 브라우저에서 로그아웃 → 재로그인 후 /admin · /admin/businesses · /admin/businesses/[id] 3개 라우트에 접근 가능하다"
    - "Business 계정이 업로드한 business-reg-docs 이미지에 대한 signed URL(TTL 3600s)이 `curl -I`로 HTTP 200을 반환하고, 같은 경로의 public URL은 400/403을 반환한다"
    - "`public.users.role = 'ADMIN'` 과 `auth.users.raw_app_meta_data->>'role' = 'ADMIN'` 이 승격된 dev 이메일에 대해 모두 true이다"
    - "Phase 7 VERIFICATION.md에 모든 커맨드 출력 원본(fenced block) + 2개 SQL 스냅샷 + signed URL 헤더 + TTL 3600s 기록이 시크릿 마스킹 상태로 존재한다"
  artifacts:
    - path: "supabase/migrations/20260414000005_phase6_admin_seed.sql"
      provides: "Admin 승격 SQL (주석 해제 + dev email 치환 커밋됨)"
      contains: "UPDATE public.users"
    - path: ".planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md"
      provides: "커맨드 출력 원본 + SQL 스냅샷 + signed URL 증거"
      contains: "prisma migrate status"
    - path: "src/generated/prisma/"
      provides: "Phase 6 컬럼 반영된 Prisma Client 재생성 산출물"
      contains: "index.d.ts"
  key_links:
    - from: "supabase/migrations/20260414000005_phase6_admin_seed.sql"
      to: "_supabase_migrations 추적 테이블"
      via: "scripts/apply-supabase-migrations.ts 실행"
      pattern: "apply-supabase-migrations"
    - from: "auth.users.raw_app_meta_data"
      to: "middleware JWT role gate"
      via: "로그아웃 → 재로그인 → 새 JWT 발급"
      pattern: "re-login"
    - from: "src/lib/supabase/storage-biz-reg.ts"
      to: "business-reg-docs bucket RLS"
      via: "createSignedUrl(path, 3600)"
      pattern: "createSignedUrl"
---

<objective>
Phase 7의 **apply 단계 (Supabase 네트워크 필수)** — D-53에 따라 prep plan(07-01)과 분리된 독립 plan. 실 Supabase에 Phase 6 마이그레이션 4건 적용 + Admin seed 실행 + Prisma 정합성 증명 + Storage RLS 왕복 검증.

- MIG-01: `apply-supabase-migrations.ts` 실행 → Phase 6 마이그레이션 000001-000005 DB 적용.
- MIG-02: `business-reg-docs` bucket RLS + signed URL 왕복 검증 (authenticated write + signed read).
- MIG-03: 승격된 Admin 계정으로 `/admin` 3 routes 접근 가능 (로그아웃 → 재로그인 필수).
- MIG-04: `prisma validate` + `prisma migrate status` + `prisma generate` clean.

Purpose: Phase 8/9/10이 실 DB 위에서 동작할 전제를 확보한다. STATE.md blocker("Supabase 접근 필요") 해소.

Output: 1개 SQL 파일 수정 + git commit + Prisma client 재생성 + `VERIFICATION.md` 원본 출력 증거 첨부.

**CRITICAL:** 이 plan은 `autonomous: false`. 최소 3개 user interaction 포함:
1. **PAUSE-1 (dev email 확인):** admin seed SQL 주석 해제 전, 승격할 dev 계정 이메일을 사용자에게 묻는다.
2. **PAUSE-2 (network 확인 + service-role 확인):** apply 스크립트 실행 전, Supabase 네트워크 도달 가능 여부 + `.env.local`에 `DATABASE_URL` / `DIRECT_URL` / `SUPABASE_SERVICE_ROLE_KEY` 가 설정되어 있는지 사용자에게 확인.
3. **PAUSE-3 (로그아웃 → 재로그인):** SQL UPDATE 적용 후 JWT 갱신을 위한 수동 재로그인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-CONTEXT.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-PATTERNS.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-VALIDATION.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-01-PLAN.md

# Canonical code references (READ these before touching files)
@supabase/migrations/20260414000005_phase6_admin_seed.sql
@scripts/apply-supabase-migrations.ts
@prisma/schema.prisma
@src/app/(auth)/role-select/actions.ts
@src/lib/supabase/storage-biz-reg.ts
@supabase/migrations/20260414000002_phase6_storage_business_reg_docs.sql
</context>

<interfaces>
<!-- Contracts the executor MUST honor. -->

**Admin seed SQL mutation scope (07-PATTERNS.md lines 26-75):**

File: `supabase/migrations/20260414000005_phase6_admin_seed.sql`
- Lines 1-21: keep verbatim (header comments).
- Lines 23-36: **uncomment** — strip leading `-- ` from every line of the BEGIN/UPDATE/COMMIT block. Replace BOTH occurrences of `admin@gignow.kr` (lines 26 and 35) with the dev email from PAUSE-1.
- Lines 38-40: keep verbatim (post-promotion JWT note).
- Lines 42-43: **DELETE** the `-- Intentionally empty ...` comment + `SELECT 1;` sentinel. These become redundant once the UPDATE block is active.

After uncomment, the active SQL block will be:
```sql
BEGIN;
  UPDATE public.users
  SET    role = 'ADMIN'
  WHERE  email = '<dev-email>';  -- from PAUSE-1

  UPDATE auth.users
  SET    raw_app_meta_data = COALESCE(raw_app_meta_data, '{}'::jsonb)
                             || jsonb_build_object('role', 'ADMIN')
  WHERE  email = '<dev-email>';  -- from PAUSE-1
COMMIT;
```

**Apply script invocation (07-RESEARCH.md §Code Examples 1):**
```bash
npx tsx scripts/apply-supabase-migrations.ts
# Uses DATABASE_URL + DIRECT_URL from .env.local (dotenv auto-load)
# _supabase_migrations 테이블로 이미 적용된 파일은 skip
```

**Prisma CLI sequence (07-RESEARCH.md §Code Examples 2):**
```bash
npx prisma validate
npx prisma migrate status
npx prisma generate
```

**Storage signed URL contract (src/lib/supabase/storage-biz-reg.ts):**
```ts
// TTL: 3600s (D-38 hardcoded — 측정치가 아닌 확정치)
supabase.storage.from('business-reg-docs').createSignedUrl(path, 3600)
```

**SQL snapshot queries for VERIFICATION.md (07-RESEARCH.md §Code Examples 4, 5):**
```sql
-- 1. public.users role check
SELECT id, email, role FROM public.users WHERE role = 'ADMIN';

-- 2. auth.users app_metadata check
SELECT id, email, raw_app_meta_data
FROM auth.users
WHERE email = '<dev-email>';
-- Expected: raw_app_meta_data contains {"role": "ADMIN", ...}

-- 3. _supabase_migrations pre-apply check (Pitfall 1 guard)
SELECT name, hash FROM _supabase_migrations
WHERE name LIKE '%admin_seed%';
-- If result has a row BEFORE the commit below, the apply script will skip this file —
-- fall back to executing the BEGIN/UPDATE/COMMIT block directly in Supabase SQL Editor.
```
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: PAUSE-1 — Ask user for dev email + PAUSE-2 — confirm Supabase network + env vars</name>
  <what-built>
    (Nothing yet — this is a pre-task gate. It collects information needed for subsequent tasks.)
  </what-built>
  <how-to-verify>
    Ask the user in the chat two questions and wait for explicit answers before proceeding:

    **Q1 (PAUSE-1):**
    > "Phase 7 Admin seed는 기존 dev 계정 1개를 ADMIN으로 승격합니다 (D-46, 신규 `admin@gignow.kr` 생성 금지).
    > 승격할 dev 계정 이메일을 알려주세요. 이 이메일은 `supabase/migrations/20260414000005_phase6_admin_seed.sql`의 2곳에 치환되어 git에 커밋됩니다.
    >
    > 주의: 이 이메일의 계정이 auth.users에 이미 존재해야 합니다 (Supabase Dashboard → Authentication → Users에서 확인).
    >
    > 이메일을 입력해주세요:"

    사용자가 이메일 `<DEV_EMAIL>`을 응답하면 후속 task에서 이 값을 사용한다.

    **Q2 (PAUSE-2):**
    > "apply 단계는 실 Supabase 네트워크가 필요합니다. 다음 3가지를 확인해주세요:
    >
    > 1. 현재 네트워크에서 Supabase 프로젝트에 접근 가능합니까? (y/n)
    > 2. `.env.local`에 `DATABASE_URL`, `DIRECT_URL`, `SUPABASE_SERVICE_ROLE_KEY`가 모두 설정되어 있습니까? (y/n)
    > 3. 승격할 dev 계정(`<DEV_EMAIL>`)이 Supabase Dashboard → Authentication → Users에 존재합니까? (y/n)
    >
    > 셋 다 'y'면 'proceed'를 입력해주세요. 하나라도 'n'이면 그 이유를 알려주세요."

    모두 'y' 및 'proceed' 응답 시 Task 2로 진행. 'n' 응답 시 Phase 7 apply를 보류하고 사용자와 해결 방안 논의.
  </how-to-verify>
  <files>
    (no files mutated — this task only gates Task 2 on user-supplied `<DEV_EMAIL>` and Supabase network confirmation)
  </files>
  <action>
    Post two chat prompts to the user (Q1 + Q2 above in `<how-to-verify>`). Block all subsequent tasks until:
    (a) user responds with a single email string `<DEV_EMAIL>` that is expected to exist in auth.users,
    (b) user responds 'proceed' confirming network + env vars + Authentication → Users presence.
    Record both responses in a working note for Task 3 to paste into VERIFICATION.md §"Pre-apply gate (PAUSE-1 + PAUSE-2)".
    If (a) or (b) returns a negative answer, HALT the plan and surface the reason to the user — do NOT proceed to Task 2.
  </action>
  <verify>
    <automated>test -n "${DEV_EMAIL:-}" 2>/dev/null || echo "DEV_EMAIL captured in session state (not an env var — verify via conversation transcript)"</automated>
    Manual: confirm the chat transcript contains one email response to Q1 and one 'proceed' response to Q2.
  </verify>
  <resume-signal>사용자가 dev email 1개 + 'proceed' 를 입력하면 resume. 'n' 응답이 있으면 halt 후 사용자 지시 대기.</resume-signal>
  <read_first>
    - .planning/phases/07-db-migration-apply-infra-foundation/07-CONTEXT.md §"Admin Seed 실행 방식" (D-44/45/46)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md §Pitfall 3 (JWT refresh)
    - .planning/STATE.md §Blockers (Supabase network access prerequisite)
  </read_first>
  <acceptance_criteria>
    - 사용자 응답에서 추출된 `<DEV_EMAIL>` 값이 이후 task의 SQL 치환에 사용 가능한 상태여야 함.
    - 'proceed' 응답이 로그에 명시적으로 기록되어야 함 (VERIFICATION.md §"Pre-apply gate"에 재기록).
  </acceptance_criteria>
  <done>
    `<DEV_EMAIL>` 확보 + 3가지 prerequisites 모두 'y' + 'proceed' 입력 완료.
  </done>
</task>

<task type="auto">
  <name>Task 2: Uncomment admin_seed.sql + substitute dev email + pre-apply NO-OP check + commit</name>
  <read_first>
    - supabase/migrations/20260414000005_phase6_admin_seed.sql (현재 NO-OP 상태 전체)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-PATTERNS.md §"supabase/migrations/20260414000005_phase6_admin_seed.sql" (line-level mutation spec)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md §Pitfall 1 (already-applied NO-OP risk)
    - src/app/(auth)/role-select/actions.ts:30-38 (app_metadata mirroring runtime pattern)
  </read_first>
  <files>
    supabase/migrations/20260414000005_phase6_admin_seed.sql (modify)
  </files>
  <action>
    **Step 1 — Pre-apply NO-OP guard (Pitfall 1):**

    Before mutating the SQL file, check if it's already recorded in `_supabase_migrations` by running (via a one-off `npx tsx` script or psql through `DIRECT_URL` — executor judgment):
    ```sql
    SELECT name FROM _supabase_migrations WHERE name LIKE '%admin_seed%';
    ```
    Paste the raw output into `VERIFICATION.md` §"Pre-apply _supabase_migrations check" (fenced block).

    **Branch A — Row 0 건 (expected):** proceed with file mutation below + apply script normal path.

    **Branch B — Row ≥ 1 건:** the file was already applied as NO-OP. Document this in VERIFICATION.md §"Pitfall 1 fallback". DO the file mutation + commit anyway (so git history is correct), but in Task 3 the apply script will skip this file → execute the BEGIN/UPDATE/COMMIT block **directly in Supabase SQL Editor** (paste the uncommented block), then capture SQL Editor output into VERIFICATION.md.

    **Step 2 — File mutation** (per 07-PATTERNS.md lines 26-75):

    Apply Edit tool to `supabase/migrations/20260414000005_phase6_admin_seed.sql`:

    a. Lines 23-36: strip leading `-- ` from each of these 14 lines:
       - Line 23: `-- BEGIN;` → `BEGIN;`
       - Lines 24-26: UPDATE public.users block → active
       - Line 27: blank (was `--`)
       - Lines 28-31: inline comment block explaining JWT mirror (KEEP as `--` inner comments — these are SQL comments inside the active block, not file-level uncomment targets)
         - Careful: lines 28-31 already have `-- ` as outer prefix. Keep ONE `-- ` (SQL comment) and strip the outer `-- ` file-comment prefix. Net result: they become proper SQL comments like `-- IMPORTANT: middleware reads JWT app_metadata.role (NOT public.users.role).`
       - Lines 32-35: UPDATE auth.users block → active
       - Line 36: `-- COMMIT;` → `COMMIT;`

    b. In the two WHERE clauses (originally lines 26 and 35 — now active SQL), replace `'admin@gignow.kr'` with `'<DEV_EMAIL>'` (using the value from Task 1 PAUSE-1). Exactly 2 occurrences.

    c. Delete lines 42-43:
       ```
       -- Intentionally empty so apply-supabase-migrations records this file without mutation.
       SELECT 1;
       ```

    d. Keep lines 1-21 (header) and 38-40 (POST-PROMOTION note) verbatim.

    **Step 3 — git commit** (required BEFORE Task 3 apply script per D-44 + Pitfall 1):
    ```bash
    git add supabase/migrations/20260414000005_phase6_admin_seed.sql
    git commit -m "feat(07): activate admin seed — promote <DEV_EMAIL> to ADMIN

    D-44 + D-46: uncomment BEGIN/UPDATE/COMMIT block and substitute dev email.
    Removes SELECT 1 sentinel. Ready for apply script (or SQL Editor fallback if
    _supabase_migrations already tracked this filename per Pitfall 1).

    Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
    ```
  </action>
  <verify>
    <automated>
      grep -q "^BEGIN;" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && grep -q "^COMMIT;" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && grep -q "UPDATE public.users" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && grep -q "UPDATE auth.users" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && ! grep -q "admin@gignow.kr" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && ! grep -q "^SELECT 1;" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && ! grep -q "Intentionally empty" supabase/migrations/20260414000005_phase6_admin_seed.sql \
      && git log -1 --name-only supabase/migrations/20260414000005_phase6_admin_seed.sql | grep -q "admin seed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^BEGIN;" supabase/migrations/20260414000005_phase6_admin_seed.sql` → exit 0
    - `grep -q "^COMMIT;" supabase/migrations/20260414000005_phase6_admin_seed.sql` → exit 0
    - `grep -c "UPDATE " supabase/migrations/20260414000005_phase6_admin_seed.sql` → `== 2` (정확히 2개 UPDATE 문)
    - `! grep -q "admin@gignow.kr" supabase/migrations/20260414000005_phase6_admin_seed.sql` → exit 0 (placeholder 제거됨)
    - `! grep -q "^SELECT 1;" supabase/migrations/20260414000005_phase6_admin_seed.sql` → exit 0 (sentinel 제거됨)
    - `<DEV_EMAIL>` 문자열이 SQL 파일에 정확히 2회 등장
    - git commit 존재 — `git log --oneline supabase/migrations/20260414000005_phase6_admin_seed.sql | head -1` 출력이 최근 커밋을 가리킴
    - VERIFICATION.md §"Pre-apply _supabase_migrations check" 섹션에 SELECT 결과가 fenced block으로 존재
  </acceptance_criteria>
  <done>
    SQL 파일이 active BEGIN/UPDATE×2/COMMIT 상태 + dev email 치환 + sentinel 제거 + git 커밋됨. Pitfall 1 pre-check 결과가 VERIFICATION.md에 기록됨.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run apply-supabase-migrations.ts + Prisma CLI trifecta + capture outputs</name>
  <read_first>
    - scripts/apply-supabase-migrations.ts (실행 스크립트 전체 — 무엇을 출력하는지 파악)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md §"Prisma CLI 검증 순서" (§Code Examples 2)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md §Pitfall 1 (already-applied 분기) §Pitfall 2 (migrate status exit code)
    - prisma/schema.prisma (validate/generate 대상)
  </read_first>
  <files>
    .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md (create),
    src/generated/prisma/ (regenerated)
  </files>
  <action>
    Execute commands in order. Capture raw stdout+stderr of each into `VERIFICATION.md` as a fenced block. Mask all secrets (`SUPABASE_SERVICE_ROLE_KEY`, JWT, password strings) as `${{SECRET}}`.

    **Step 1 — Apply migrations:**
    ```bash
    npx tsx scripts/apply-supabase-migrations.ts 2>&1 | tee /tmp/apply.log
    ```
    - Expected success output: `Done. N applied, M skipped (already applied).`
    - Branch A (Task 2 NO-OP check returned 0 rows): at least 1 "Applying: ...admin_seed.sql" line in output.
    - Branch B (Task 2 NO-OP check returned ≥1 row): output shows `Skipping (already applied): ...admin_seed.sql`. → Follow Pitfall 1 fallback: paste the uncommented BEGIN/UPDATE/COMMIT block into Supabase SQL Editor, execute, copy the output into VERIFICATION.md §"Admin seed — SQL Editor fallback (Pitfall 1 Branch B)".

    Paste the FULL output of `/tmp/apply.log` into VERIFICATION.md §"apply-supabase-migrations output".

    **Step 2 — Prisma validate:**
    ```bash
    npx prisma validate 2>&1 | tee /tmp/prisma-validate.log
    ```
    Expected: `The schema at prisma/schema.prisma is valid 🚀` + exit 0.
    Paste full output into VERIFICATION.md §"prisma validate output".

    **Step 3 — Prisma migrate status:**
    ```bash
    npx prisma migrate status 2>&1 | tee /tmp/prisma-status.log ; echo "EXIT=$?"
    ```
    - Expected success: exit 0 + "Database schema is up to date!" message.
    - **If exit 1 (Pitfall 2):** Document the exact error in VERIFICATION.md §"migrate status — drift observation". Do NOT continue to Step 4/5. Halt and route to `/gsd-debug` with the error output — per D-53 "Claude's Discretion — drift 복구는 실행 시점 판단".
    - Note on interpretation (Pitfall 2 / Pitfall 3): `_prisma_migrations` 테이블이 비어있거나 없을 때 "no migration table is found" 메시지가 나올 수 있다. 이는 direct-prisma 전략의 예상 상태 — `exit 0 + "Database schema is up to date!"` 만을 clean으로 간주.

    Paste full output + exit code into VERIFICATION.md §"prisma migrate status output".

    **Step 4 — Prisma generate:**
    ```bash
    npx prisma generate 2>&1 | tee /tmp/prisma-generate.log
    ```
    Expected: `Generated Prisma Client (...) to ./src/generated/prisma` + exit 0.
    Paste full output into VERIFICATION.md §"prisma generate output".

    **Step 5 — VERIFICATION.md scaffolding:**
    The file should have these section headings (create H2 sections in this order):
    ```markdown
    # Phase 7 — Verification Evidence

    **Phase:** 07-db-migration-apply-infra-foundation
    **Applied:** <YYYY-MM-DD>
    **Dev email promoted:** <DEV_EMAIL>  (※ SUPABASE_SERVICE_ROLE_KEY 및 JWT는 ${{SECRET}}로 마스킹)

    ## Pre-apply gate (PAUSE-1 + PAUSE-2)
    <user responses from Task 1>

    ## Pre-apply _supabase_migrations check (Pitfall 1)
    ```sql
    SELECT name FROM _supabase_migrations WHERE name LIKE '%admin_seed%';
    ```
    <fenced output>

    ## apply-supabase-migrations output
    <fenced bash output>

    ## Admin seed — SQL Editor fallback (Pitfall 1 Branch B)
    <only if Branch B was taken, else write "N/A — Branch A (normal apply) was used.">

    ## prisma validate output
    <fenced output>

    ## prisma migrate status output
    <fenced output + EXIT code line>

    ## prisma generate output
    <fenced output>

    ## [Task 4 placeholders — filled in next task]
    ## public.users + auth.users snapshot (ADMIN)
    ## Storage signed URL round-trip
    ## Admin login /admin 3-route verification
    ```

    Secret masking rule: if any fenced block contains a `eyJ...` JWT, a `service_role` literal value, or a bcrypt hash, replace the value with `${{SECRET}}`. URL structure (domain + path) may remain.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
      && grep -q "apply-supabase-migrations output" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
      && grep -q "prisma validate output" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
      && grep -q "prisma migrate status output" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
      && grep -q "prisma generate output" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
      && grep -q "Pre-apply _supabase_migrations check" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
      && test -d src/generated/prisma \
      && test -f src/generated/prisma/index.d.ts \
      && ! grep -qE "eyJ[A-Za-z0-9_-]{20,}" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exit 0 (captured in VERIFICATION.md)
    - `npx prisma migrate status` exit 0 OR documented halt-to-gsd-debug with error (Pitfall 2 branch)
    - `npx prisma generate` exit 0 + `src/generated/prisma/index.d.ts` 존재
    - apply 스크립트 출력에 `Done. N applied, M skipped` 또는 Pitfall 1 Branch B fallback 증거 존재
    - VERIFICATION.md의 어떤 fenced block에도 JWT raw (`eyJ...` 20자 이상), `SUPABASE_SERVICE_ROLE_KEY` 실 값, DB password 실 값이 포함되지 않음 (모두 `${{SECRET}}` 마스킹)
    - VERIFICATION.md §"Pre-apply _supabase_migrations check" 섹션에 SELECT 결과 fenced block 존재
  </acceptance_criteria>
  <done>
    4개 커맨드 모두 clean 실행 + 출력이 VERIFICATION.md fenced block으로 기록 + 시크릿 0건 유출 + Pitfall 1/2 분기 명시.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: PAUSE-3 — Logout/re-login + /admin 3-route access + Storage signed URL round-trip + SQL snapshots</name>
  <what-built>
    Task 2-3에서 admin seed 적용 + Prisma 정합성 증명 완료. 이 checkpoint는 JWT refresh 경로 + Storage RLS 왕복을 수동으로 검증하고, VERIFICATION.md 나머지 3개 섹션(§"public.users + auth.users snapshot", §"Storage signed URL round-trip", §"Admin login /admin 3-route verification")을 채운다.
  </what-built>
  <how-to-verify>
    **Step A — SQL snapshots (executor가 수행, user observes):**

    Run these queries via `npx tsx` script with `DIRECT_URL` or paste into Supabase SQL Editor. Paste results into VERIFICATION.md §"public.users + auth.users snapshot (ADMIN)":

    ```sql
    -- 1. public.users
    SELECT id, email, role FROM public.users WHERE role = 'ADMIN';
    -- Expected: exactly 1 row with email = <DEV_EMAIL>

    -- 2. auth.users (service_role required)
    SELECT id, email, raw_app_meta_data
    FROM auth.users
    WHERE email = '<DEV_EMAIL>';
    -- Expected: raw_app_meta_data contains "role": "ADMIN"
    ```

    Mask any JWT / token values in the output as `${{SECRET}}`.

    **Step B — User logout + re-login (PAUSE-3, Pitfall 3):**

    Show the user this message and wait for confirmation:

    > "SQL에서 role은 업데이트되었지만 JWT는 아직 갱신되지 않았습니다 (Pitfall 3).
    >
    > 다음을 수행해주세요:
    > 1. 브라우저에서 현재 GigNow 세션 **로그아웃**
    > 2. 승격한 dev 이메일 `<DEV_EMAIL>`로 **재로그인**
    > 3. 다음 3개 경로에 접근 시도하고 각각의 결과를 알려주세요:
    >    - `/admin` — 렌더링 성공? (y/n)
    >    - `/admin/businesses` — 렌더링 성공? (y/n)
    >    - `/admin/businesses/[id]` — 임의의 business id로 접근, 렌더링 성공? (y/n)
    >
    > 3개 모두 'y'면 'admin-verified' 를 입력해주세요."

    3개 모두 성공 시 VERIFICATION.md §"Admin login /admin 3-route verification"에 다음 형식으로 기록:
    ```markdown
    - Logout time: <HH:MM>
    - Re-login time: <HH:MM>
    - /admin: PASS (no redirect to /login)
    - /admin/businesses: PASS
    - /admin/businesses/[id]: PASS (tested with business id = <anonymized-id>)
    ```

    어느 하나라도 FAIL이면 Pitfall 3 재적용 (세션 캐시 지우기 + 쿠키 초기화) 또는 `_supabase_migrations` 상태 재확인 (Pitfall 1 Branch B 가능성).

    **Step C — Storage signed URL round-trip (MIG-02, T-MIG-02):**

    Ask the user to:
    > "business-reg-docs bucket RLS 왕복 검증이 필요합니다.
    > 1. 로컬 dev 서버에서 Business 계정으로 로그인 → `/biz/verify` 에서 사업자등록증 이미지 업로드 (test 이미지 사용).
    > 2. 업로드 성공하면 파일 경로를 알려주세요 (예: `<user-id>/<biz-id>.jpg`)."

    경로 수신 후 executor가 다음을 수행:

    ```bash
    # (1) signed URL 발급 — npx tsx 스크립트로 createSignedUrl(path, 3600) 호출
    #     또는 Supabase SQL Editor에서 `SELECT storage.create_signed_url(...)` 사용 (executor 판단)
    npx tsx -e "
      import { createClient } from '@supabase/supabase-js';
      const s = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
      const { data } = await s.storage.from('business-reg-docs').createSignedUrl('<PATH>', 3600);
      console.log(data.signedUrl);
    "
    # → 출력: signed URL

    # (2) curl: signed URL (expect 200)
    curl -I "<signed_url>" 2>&1
    # Expected: HTTP/2 200, Content-Type: image/jpeg (or image/png, application/pdf)

    # (3) curl: public URL path (expect 400/403 — RLS block)
    curl -I "https://<project>.supabase.co/storage/v1/object/public/business-reg-docs/<PATH>" 2>&1
    # Expected: 400 or 403
    ```

    VERIFICATION.md §"Storage signed URL round-trip" 에 다음을 기록:
    - signed URL 구조 (도메인 + path만; query string의 token은 `?token=${{SECRET}}`로 마스킹)
    - `curl -I` 응답 헤더 전체 (signed URL → 200, public URL → 4xx)
    - `TTL: 3600s (D-38 hardcoded in src/lib/supabase/storage-biz-reg.ts — confirmed, not measured)`
    - 이 수치는 Phase 8 INFRA-02에서 재사용됨을 주석.
  </how-to-verify>
  <action>
    Execute the 3 steps (A/B/C) documented above in `<how-to-verify>`:
    (A) Run 2 SQL snapshot queries (public.users role + auth.users raw_app_meta_data) via `npx tsx` or SQL Editor; paste masked results into VERIFICATION.md §"public.users + auth.users snapshot (ADMIN)".
    (B) Prompt the user to logout → re-login with `<DEV_EMAIL>` and test 3 admin routes; record timestamps + PASS/FAIL per route in VERIFICATION.md §"Admin login /admin 3-route verification".
    (C) Ask user to upload a test business-reg image via /biz/verify; then executor issues `createSignedUrl(path, 3600)`, runs `curl -I` against signed URL (expect 200) and public URL (expect 4xx); record headers + `TTL: 3600s (D-38 hardcoded)` into VERIFICATION.md §"Storage signed URL round-trip". Mask any token/JWT as `${{SECRET}}`.
    If any step fails (admin redirect, signed URL non-200, RLS leak), pause and diagnose per Pitfall 1/3/4 branches — do NOT finalize VERIFICATION.md until all 3 pass.
  </action>
  <verify>
    <automated>
      grep -q "public.users + auth.users snapshot" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md       && grep -q "Storage signed URL round-trip" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md       && grep -q "Admin login /admin 3-route verification" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md       && grep -q "TTL: 3600s" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md       && grep -qE "HTTP/(1\.1|2) 200" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md       && ! grep -qE "eyJ[A-Za-z0-9_-]{20,}" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md
    </automated>
  </verify>
  <resume-signal>사용자가 'admin-verified' 를 입력하고 Storage 업로드 경로를 제공한 후, executor가 signed URL 왕복 검증을 완료하고 VERIFICATION.md 3개 섹션을 모두 채웠다면 resume.</resume-signal>
  <read_first>
    - .planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md §Pitfall 3 (JWT re-login) §Pitfall 4 (Storage session) §Code Examples 3 (signed URL)
    - supabase/migrations/20260414000002_phase6_storage_business_reg_docs.sql (RLS 4 정책)
    - src/lib/supabase/storage-biz-reg.ts (`createSignedUrl(path, 3600)` — TTL source of truth)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-CONTEXT.md §"증거 형식" (D-55 마스킹 규칙)
  </read_first>
  <files>
    .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md (update — 3 sections appended)
  </files>
  <acceptance_criteria>
    - VERIFICATION.md §"public.users + auth.users snapshot (ADMIN)" 에 2개 SQL 결과 fenced block 존재
    - public.users 스냅샷에 `role` 컬럼 값 `ADMIN` 확인 가능
    - auth.users 스냅샷에 `raw_app_meta_data` JSONB에 `"role": "ADMIN"` 문자열 존재
    - VERIFICATION.md §"Admin login /admin 3-route verification" 에 3개 경로 PASS 로그 존재
    - VERIFICATION.md §"Storage signed URL round-trip" 에 signed URL 응답 200 + public URL 응답 400/403 + `TTL: 3600s` 라인 존재
    - `grep -q "TTL: 3600s" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md` → exit 0
    - `grep -q "HTTP/2 200\|HTTP/1.1 200" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md` → exit 0
    - VERIFICATION.md 전체에 `eyJ[A-Za-z0-9_-]{20,}` 패턴 0건 (JWT 미노출 재확인)
    - `<DEV_EMAIL>` 외 실 이메일 / 실 biz id 는 anonymize (`<biz-id>` 등)된 상태
  </acceptance_criteria>
  <done>
    3개 섹션 모두 VERIFICATION.md에 기록됨 + JWT 재로그인 완료 + /admin 3 routes PASS + Storage signed URL 왕복 검증 PASS + TTL 3600s 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev workstation ↔ Supabase Postgres | `DIRECT_URL` + service_role로 DDL/DML 실행. 서비스 롤 키는 .env.local (gitignore)에만. |
| Authenticated Business user session ↔ business-reg-docs bucket | RLS enforced: INSERT/SELECT 모두 `auth.uid()::text = (storage.foldername(name))[1]` 요구. |
| Admin (role=ADMIN) JWT ↔ /admin middleware | middleware가 JWT app_metadata.role을 읽음. DB UPDATE 후 logout → re-login 없으면 gate 통과 불가. |
| VERIFICATION.md markdown output ↔ git public history | 모든 시크릿은 `${{SECRET}}`으로 치환. 공개 URL 구조는 허용. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MIG-02 | Information Disclosure | business-reg-docs bucket (private) | mitigate | Task 4 Step C가 signed URL (200) + public URL (4xx) 둘 다 curl로 검증하고 VERIFICATION.md에 응답 헤더 기록. RLS 4 정책은 이미 `20260414000002_phase6_storage_business_reg_docs.sql`에 정의됨 — Task 3의 apply 스크립트가 이를 적용. TTL 3600s는 `src/lib/supabase/storage-biz-reg.ts`에 하드코딩 (노출 창 bounded). |
| T-MIG-03 | Elevation of Privilege | admin seed SQL (public.users.role + auth.users.raw_app_meta_data) | mitigate | Task 1 PAUSE-1이 dev email을 사용자에게 명시적으로 확인 (placeholder `admin@gignow.kr` 우회 불가). Task 2는 git commit으로 이메일 변경을 이력에 남김. D-45에 따라 v1.1은 dev 환경만 승격 — prod은 v1.2 ops phase로 이월. Pitfall 3 로그아웃/재로그인 절차로 JWT 갱신 없이 Admin 권한 탈취 불가. |
| T-INFRA-01 | Information Disclosure | VERIFICATION.md git 이력 | mitigate | Task 3 `<action>`에 시크릿 마스킹 규칙 명시 (`eyJ...` JWT, `service_role` 값, bcrypt hash 모두 `${{SECRET}}` 치환). Task 3-4의 acceptance_criteria에 `! grep -qE "eyJ[A-Za-z0-9_-]{20,}"` 자동 가드. 공개 URL 도메인/경로는 허용 (토큰 없는 구조). |
| T-07P02-04 | Tampering | `_supabase_migrations` 테이블 | accept | PRIMARY KEY가 filename이므로 중복 apply 자동 차단. Pitfall 1 Branch B 경로 (SQL Editor fallback)는 파일 apply를 건너뛰지만 UPDATE 결과는 동일. |
</threat_model>

<verification>
Overall phase check (run after all 4 tasks complete):
```bash
# 1. All VERIFICATION.md sections present
for section in "Pre-apply _supabase_migrations check" "apply-supabase-migrations output" \
               "prisma validate output" "prisma migrate status output" "prisma generate output" \
               "public.users + auth.users snapshot" "Storage signed URL round-trip" \
               "Admin login /admin 3-route verification"; do
  grep -q "$section" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
    || { echo "MISSING: $section"; exit 1; }
done

# 2. Secret masking guard
! grep -qE "eyJ[A-Za-z0-9_-]{20,}" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md \
  || { echo "LEAKED JWT"; exit 1; }

# 3. TTL recorded
grep -q "TTL: 3600s" .planning/phases/07-db-migration-apply-infra-foundation/VERIFICATION.md

# 4. Prisma client regenerated
test -f src/generated/prisma/index.d.ts

# 5. Admin seed committed with dev email (not placeholder)
! grep -q "admin@gignow.kr" supabase/migrations/20260414000005_phase6_admin_seed.sql
grep -q "UPDATE auth.users" supabase/migrations/20260414000005_phase6_admin_seed.sql

echo "07-02 apply: all gates green"
```
</verification>

<success_criteria>
ROADMAP §Phase 7 Success Criteria mapping:
1. **SC-1 (prisma clean)** → Task 3 (validate + migrate status + generate 모두 exit 0, VERIFICATION.md fenced)
2. **SC-2 (signed URL RLS)** → Task 4 Step C (business-reg-docs upload → signed URL 200 → public URL 4xx → TTL 3600s)
3. **SC-3 (admin /admin 3 routes)** → Task 4 Step B (logout → re-login → 3 routes PASS)
4. **SC-4 (CLOVA 5분 가이드)** → 07-01 plan에서 완료 (INFRA-01 prep). 이 plan은 해당 없음.
5. **SC-5 (MOCK-LOG 템플릿)** → 07-01 plan에서 완료 (INFRA-03 prep). 이 plan은 해당 없음.

Requirement IDs closed by this plan: MIG-01, MIG-02, MIG-03, MIG-04.
</success_criteria>

<output>
After completion, create `.planning/phases/07-db-migration-apply-infra-foundation/07-02-SUMMARY.md` documenting:
- Admin seed commit hash + dev email hash (or `<anonymized>`)
- Migrations applied count (N applied + M skipped)
- `prisma migrate status` clean state confirmation
- Storage signed URL TTL = 3600s (reference for Phase 8 INFRA-02)
- 3 manual checkpoints all PASS (PAUSE-1 email, PAUSE-2 network, PAUSE-3 re-login)
- Any Pitfall 1/2/3 branches encountered + how they were resolved
- Phase 7 close state: STATE.md blocker("Supabase 접근 필요") 해소, Phase 8/9/10 unblocked.
</output>

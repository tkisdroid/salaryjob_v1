---
phase: 07-db-migration-apply-infra-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/templates/MOCK-LOG.md
  - .planning/MOCK-INDEX.md
  - .planning/README.md
  - .planning/phases/06-admin-backoffice/MOCK-LOG.md
  - docs/external-keys.md
  - .env.example
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
user_setup: []
tags:
  - docs
  - mock-policy
  - infra

must_haves:
  truths:
    - "신규 개발자가 `.env.example` 주석에서 `docs/external-keys.md` 링크를 따라가 CLOVA OCR 5분 체크리스트를 읽을 수 있다"
    - "`.planning/templates/MOCK-LOG.md`가 4필드(Mocked path / Reason / Real-key re-verify step / Target milestone)를 고정된 표 형식으로 정의한다"
    - "`.planning/MOCK-INDEX.md`가 Phase 6 deferred 시나리오 1건 이상을 한 행으로 포함한다"
    - "`.planning/README.md`에 MOCK 정책과 템플릿 사용법 섹션이 있다"
    - "`.planning/phases/06-admin-backoffice/MOCK-LOG.md`가 Phase 6 deferred 3 시나리오(admin detail signed image / OCR happy / OCR mismatch)를 pre-populate한 상태로 존재한다"
  artifacts:
    - path: ".planning/templates/MOCK-LOG.md"
      provides: "4필드 MOCK-LOG 표준 템플릿"
      contains: "Mocked path"
    - path: ".planning/MOCK-INDEX.md"
      provides: "밀스톤 루트 MOCK 집계 인덱스"
      contains: "Phase"
    - path: ".planning/README.md"
      provides: "MOCK 정책 + 템플릿 사용법"
      contains: "MOCK 정책"
    - path: ".planning/phases/06-admin-backoffice/MOCK-LOG.md"
      provides: "Phase 6 deferred 3 시나리오 MOCK 기록"
      contains: "Phase 6 Sc."
    - path: "docs/external-keys.md"
      provides: "CLOVA OCR 5분 발급 체크리스트"
      contains: "CLOVA_OCR_SECRET"
    - path: ".env.example"
      provides: ".env 주석에 외부 키 가이드 링크"
      contains: "docs/external-keys.md"
  key_links:
    - from: ".env.example"
      to: "docs/external-keys.md"
      via: "# Provisioning guide 주석 한 줄"
      pattern: "docs/external-keys.md"
    - from: ".planning/MOCK-INDEX.md"
      to: ".planning/phases/06-admin-backoffice/MOCK-LOG.md"
      via: "markdown relative link"
      pattern: "phases/06-admin-backoffice/MOCK-LOG.md"
    - from: ".planning/README.md"
      to: ".planning/templates/MOCK-LOG.md"
      via: "본문에서 템플릿 경로 명시"
      pattern: "templates/MOCK-LOG.md"
---

<objective>
Phase 7의 **prep 단계 (네트워크 불필요)** — D-53에 따라 apply 단계(07-02)와 분리된 독립 plan. Supabase 네트워크가 닫혀 있어도 끝까지 실행 + 커밋 가능한 산출물만 다룬다.

- INFRA-03: MOCK-LOG 템플릿 + 밀스톤 인덱스 + README 정책 섹션 + Phase 6 deferred 3건 pre-populate (D-49/50/51/52)
- INFRA-01: `docs/external-keys.md` (docs/ 폴더 신설) + `.env.example` 1-line annotation (D-47/48)

Purpose: 후속 phase(8/10)가 재사용할 공통 MOCK 인프라와 외부 키 프로비저닝 문서를 apply 단계와 독립적으로 확립한다.
Output: 5개 신규 파일 + `.env.example` 1-line 수정. 커밋 후 07-02 plan이 동일 브랜치에서 실행된다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-CONTEXT.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-RESEARCH.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-PATTERNS.md
@.planning/phases/07-db-migration-apply-infra-foundation/07-VALIDATION.md

# Existing analogs (pattern sources — do NOT modify in this plan)
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.env.example
@src/lib/ocr/clova.ts
</context>

<interfaces>
<!-- Content specs that this plan's executors must follow verbatim. -->

**CLOVA env var contract (from src/lib/ocr/clova.ts):**
```ts
// src/lib/ocr/clova.ts:42-50 (do NOT modify — the docs must match these exact names)
const secret = process.env.CLOVA_OCR_SECRET
const apiUrl = process.env.CLOVA_OCR_API_URL
// Both env vars must be present for OCR to run; otherwise graceful-fail (skip).
```

**MOCK-LOG 4 mandatory fields (D-50 — verbatim):**
| Field | Value |
|-------|-------|
| Mocked path | [시나리오 이름 + 무엇을 mock했는지] |
| Reason | [외부 의존성이 없어 mock한 이유] |
| Real-key re-verify step | [실 키 확보 후 실행할 구체적 명령/단계] |
| Target milestone | [v1.2 또는 v2] |

**Existing .env.example CLOVA section (lines 65-70 — annotate only):**
```
# ============================================================================
# Phase 6 — Admin backoffice & OCR
# ============================================================================
# Naver CLOVA OCR General (biz license extraction) — https://guide.ncloud-docs.com/docs/en/clovaocr-overview
# Leave blank to disable OCR (D-33 graceful degradation: image still saves, admin review flag set)
CLOVA_OCR_SECRET=
CLOVA_OCR_API_URL=
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MOCK-LOG infrastructure (template + index + README + Phase 6 instance)</name>
  <read_first>
    - .planning/phases/07-db-migration-apply-infra-foundation/07-CONTEXT.md §Specific Ideas (4-field example table, lines 149-157)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-PATTERNS.md §".planning/templates/MOCK-LOG.md" §".planning/MOCK-INDEX.md" §".planning/README.md" §".planning/phases/06-admin-backoffice/MOCK-LOG.md"
    - .planning/REQUIREMENTS.md §"MOCK 정책" + §INFRA-03
    - .planning/ROADMAP.md §"MOCK Policy (milestone-wide)"
    - .planning/PROJECT.md (voice/heading style analog)
  </read_first>
  <files>
    .planning/templates/MOCK-LOG.md (create),
    .planning/MOCK-INDEX.md (create),
    .planning/README.md (create),
    .planning/phases/06-admin-backoffice/MOCK-LOG.md (create)
  </files>
  <action>
    Create 4 files in one logical task group (all markdown, all in .planning/, no code).

    **(1) `.planning/templates/MOCK-LOG.md`** (per D-49/50 + 07-PATTERNS.md lines 80-114):
    - Header: `# MOCK-LOG: [Phase Name]` + two metadata lines (`**Phase:**` + `**Template source:** \`.planning/templates/MOCK-LOG.md\``)
    - HTML comment: `<!-- Add one entry block per mocked scenario. Repeat the table below. -->`
    - Followed by `## Entry: [Scenario Name]` with the 4-field table (Mocked path / Reason / Real-key re-verify step / Target milestone — EXACTLY these 4, in this order, in Korean+English mix per project convention).
    - Do NOT add date / owner / PR-link fields — D-50 explicitly excludes them.

    **(2) `.planning/MOCK-INDEX.md`** (per D-51 + 07-PATTERNS.md lines 118-140):
    - Header: `# MOCK-INDEX` + `**Milestone:** v1.1 Ship-Ready` + 1-sentence purpose line.
    - Markdown pipe table with columns: `Phase | Mocked path | Reason | Target milestone | MOCK-LOG`.
    - Pre-populate with 3 Phase 6 rows (one per deferred scenario — same content as the 3 entries in Phase 6 MOCK-LOG below). Each row's `MOCK-LOG` column links to `phases/06-admin-backoffice/MOCK-LOG.md` (relative from .planning/).

    **(3) `.planning/README.md`** (per D-52 + 07-PATTERNS.md lines 144-162):
    - File does NOT yet exist — create from scratch.
    - Brief project orientation (3-5 lines: "`.planning/` 는 GSD 워크플로 planning artifact 루트. 밀스톤 스냅샷 / 페이즈별 산출물 / 공용 템플릿을 보관한다.")
    - H2 section `## MOCK 정책 및 템플릿 사용법` containing:
      1. 한 문단: MOCK-LOG이 무엇이고 왜 존재하는지 (외부 API 키 없이 완료 불가한 시나리오를 추적하기 위한 기록).
      2. 4 mandatory fields를 bullet로 나열 (D-50 문구 정렬): Mocked path / Reason / Real-key re-verify step / Target milestone.
      3. 템플릿 경로 명시: `` `.planning/templates/MOCK-LOG.md` ``.
      4. 사용 절차 (번호 리스트): 템플릿 복사 → `phases/<phase>/MOCK-LOG.md`로 복제 → 4필드 채움 → `.planning/MOCK-INDEX.md`에 한 행 추가.
      5. 언제 항목을 만드는지: ROADMAP §"MOCK Policy" 문구 인용 ("외부 API 키(CLOVA OCR, 결제 등) 없이 완료 불가한 시나리오").
    - D-52 constraint: 섹션 전체 ~30 lines 이내 (1쪽 분량).

    **(4) `.planning/phases/06-admin-backoffice/MOCK-LOG.md`** (per 07-PATTERNS.md lines 166-208):
    - Instantiate template. `**Phase:** 06 — admin-backoffice`.
    - 3 `## Entry:` blocks, each with the 4-field table, EXACTLY as specified in 07-PATTERNS.md (Entry 1: Sc.9 Admin Detail Signed Image [target v1.1 — Phase 7 apply에서 해소], Entry 2: Sc.7 CLOVA OCR Happy-Path [target v1.2], Entry 3: Sc.8 CLOVA OCR Mismatch Graceful Degradation [target v1.2]).
    - Copy entry content verbatim from 07-PATTERNS.md lines 173-206 (do not paraphrase — those strings are canonical).
  </action>
  <verify>
    <automated>
      test -f .planning/templates/MOCK-LOG.md \
      && test -f .planning/MOCK-INDEX.md \
      && test -f .planning/README.md \
      && test -f .planning/phases/06-admin-backoffice/MOCK-LOG.md \
      && grep -q "Mocked path" .planning/templates/MOCK-LOG.md \
      && grep -q "Real-key re-verify step" .planning/templates/MOCK-LOG.md \
      && grep -q "Target milestone" .planning/templates/MOCK-LOG.md \
      && grep -q "Reason" .planning/templates/MOCK-LOG.md \
      && grep -q "MOCK 정책" .planning/README.md \
      && grep -q "templates/MOCK-LOG.md" .planning/README.md \
      && grep -q "phases/06-admin-backoffice/MOCK-LOG.md" .planning/MOCK-INDEX.md \
      && grep -c "^## Entry:" .planning/phases/06-admin-backoffice/MOCK-LOG.md | awk '$1>=3{exit 0}{exit 1}' \
      && grep -q "Phase 6 Scenario 9" .planning/phases/06-admin-backoffice/MOCK-LOG.md \
      && grep -q "CLOVA_OCR_SECRET" .planning/phases/06-admin-backoffice/MOCK-LOG.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/templates/MOCK-LOG.md` → exit 0
    - `test -f .planning/MOCK-INDEX.md` → exit 0
    - `test -f .planning/README.md` → exit 0
    - `test -f .planning/phases/06-admin-backoffice/MOCK-LOG.md` → exit 0
    - `grep -c "Mocked path" .planning/templates/MOCK-LOG.md` → `>= 1`
    - `grep -q "MOCK 정책" .planning/README.md` → exit 0
    - `grep -c "^## Entry:" .planning/phases/06-admin-backoffice/MOCK-LOG.md` → `>= 3`
    - `.planning/MOCK-INDEX.md` contains markdown pipe table header `| Phase | Mocked path |`
    - `.planning/README.md` total line count ≤ 60 (D-52 1쪽 분량 constraint)
  </acceptance_criteria>
  <done>
    4개 파일 모두 존재 + 4필드가 템플릿/Phase 6 인스턴스에 모두 포함 + README가 템플릿 경로와 MOCK-INDEX를 언급 + MOCK-INDEX가 Phase 6 3 행을 포함.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/external-keys.md CLOVA guide + annotate .env.example</name>
  <read_first>
    - .planning/phases/07-db-migration-apply-infra-foundation/07-CONTEXT.md §"CLOVA_OCR_SECRET 문서화" (D-47, D-48)
    - .planning/phases/07-db-migration-apply-infra-foundation/07-PATTERNS.md §"docs/external-keys.md" §".env.example"
    - .planning/REQUIREMENTS.md §INFRA-01
    - src/lib/ocr/clova.ts:42-50 (env var contract — `CLOVA_OCR_SECRET` + `CLOVA_OCR_API_URL` 정확한 이름)
    - .env.example lines 65-70 (existing CLOVA section pattern to extend)
  </read_first>
  <files>
    docs/external-keys.md (create — docs/ folder also new),
    .env.example (modify — single annotation line)
  </files>
  <action>
    **(1) Create `docs/external-keys.md`** (per D-47/48 + 07-PATTERNS.md lines 222-251):

    Create the `docs/` folder (first file in it). File content (verbatim per 07-PATTERNS.md):

    ```markdown
    # External Keys Provisioning Guide

    > 외부 API 키 발급 가이드. 스크린샷 없음 — NCP 콘솔 UI가 변경되어도 유효하도록 텍스트 체크리스트로만 작성.

    ## CLOVA OCR (사업자등록증 번호 추출)

    **환경 변수:** `CLOVA_OCR_SECRET`, `CLOVA_OCR_API_URL`
    **소요 시간:** 약 5분
    **참고:** https://guide.ncloud-docs.com/docs/en/clovaocr-overview

    ### 발급 절차

    - [ ] 1. NCP 콘솔(https://console.ncloud.com) 가입 또는 로그인
    - [ ] 2. AI Services → CLOVA OCR → 도메인 목록 → 도메인 생성
          - 도메인 유형: **General** (특화 모델 불필요)
          - 이름: 임의 (예: `gignow-biz-reg`)
    - [ ] 3. 생성된 도메인 클릭 → **API Gateway 연동** 탭 → Invoke URL 복사
          → `.env.local`에 `CLOVA_OCR_API_URL=<복사한 Invoke URL>` 입력
    - [ ] 4. 동일 탭에서 **X-OCR-SECRET** 값 복사
          → `.env.local`에 `CLOVA_OCR_SECRET=<복사한 Secret>` 입력
    - [ ] 5. Next.js 개발 서버 재시작 (`npm run dev`)
    - [ ] 6. `/biz/verify` 에서 사업자등록증 이미지 업로드 → 콘솔 로그에서
          `CLOVA OCR` 응답 확인 (에러 없이 regNumber 추출되면 성공)

    > 키 없이 실행하면 `src/lib/ocr/clova.ts` 가 graceful fail(skip)을 반환하고
    > `regNumberOcrMismatched` 는 기본값 `false` 유지. OCR 없이도 이미지 저장은 정상 동작.
    ```

    D-48 constraint: 스크린샷/이미지 embed 절대 금지. NCP 공식 문서 링크만.

    **(2) Modify `.env.example`** — single line annotation:

    Between existing line 68 (`# Leave blank to disable OCR ...`) and line 69 (`CLOVA_OCR_SECRET=`), insert exactly ONE new line:

    ```
    # Provisioning guide: docs/external-keys.md §CLOVA OCR
    ```

    Do NOT change variable names. Do NOT change blank values. Do NOT touch any other lines.

    Before (lines 67-70):
    ```
    # Naver CLOVA OCR General (biz license extraction) — https://guide.ncloud-docs.com/docs/en/clovaocr-overview
    # Leave blank to disable OCR (D-33 graceful degradation: image still saves, admin review flag set)
    CLOVA_OCR_SECRET=
    CLOVA_OCR_API_URL=
    ```

    After (lines 67-71):
    ```
    # Naver CLOVA OCR General (biz license extraction) — https://guide.ncloud-docs.com/docs/en/clovaocr-overview
    # Leave blank to disable OCR (D-33 graceful degradation: image still saves, admin review flag set)
    # Provisioning guide: docs/external-keys.md §CLOVA OCR
    CLOVA_OCR_SECRET=
    CLOVA_OCR_API_URL=
    ```
  </action>
  <verify>
    <automated>
      test -f docs/external-keys.md \
      && grep -q "CLOVA_OCR_SECRET" docs/external-keys.md \
      && grep -q "CLOVA_OCR_API_URL" docs/external-keys.md \
      && grep -q "https://console.ncloud.com" docs/external-keys.md \
      && grep -q "https://guide.ncloud-docs.com/docs/en/clovaocr-overview" docs/external-keys.md \
      && ! grep -qE "!\[.*\]\(" docs/external-keys.md \
      && grep -c "^- \[ \]" docs/external-keys.md | awk '$1>=6{exit 0}{exit 1}' \
      && grep -q "docs/external-keys.md" .env.example \
      && grep -q "CLOVA_OCR_SECRET=" .env.example \
      && grep -q "CLOVA_OCR_API_URL=" .env.example
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/external-keys.md` → exit 0
    - `grep -q "CLOVA_OCR_SECRET" docs/external-keys.md` → exit 0
    - `grep -q "CLOVA_OCR_API_URL" docs/external-keys.md` → exit 0
    - `grep -c "^- \[ \]" docs/external-keys.md` → `>= 6` (6단계 체크리스트)
    - `! grep -qE "!\[.*\]\(" docs/external-keys.md` → exit 0 (no image embeds — D-48)
    - `grep -q "docs/external-keys.md" .env.example` → exit 0
    - `.env.example`의 `CLOVA_OCR_SECRET=` 라인과 `CLOVA_OCR_API_URL=` 라인은 여전히 빈 값으로 유지
    - `.env.example` diff = exactly 1 line added (no other mutations)
  </acceptance_criteria>
  <done>
    `docs/external-keys.md`가 6-step 체크리스트 + 환경 변수 2개 + NCP 공식 링크를 포함. `.env.example` 주석에 `docs/external-keys.md §CLOVA OCR` 한 줄이 추가되고 값은 모두 비어있다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo ↔ developer workstation | 문서 파일만 추가. 외부 신뢰 경계 없음. |
| `.env.example` ↔ `.env.local` | `.env.example`은 placeholder만; 실제 시크릿은 `.env.local`(gitignore)로만 전달. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-INFRA-01 | Information Disclosure | `.env.example` / `docs/external-keys.md` | mitigate | 두 파일은 placeholder + 발급 절차만 기록. 실 값(`CLOVA_OCR_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`) 커밋 금지 — Task 2의 acceptance에 `CLOVA_OCR_SECRET=` 빈 값 유지 grep 가드. 가이드는 발급 절차만 지시하고 실 시크릿 값 예시 0건. |
| T-07P01-02 | Tampering | MOCK-LOG 템플릿 / 인스턴스 | accept | 단일 개발자 + git 이력으로 변경 추적. 추가적 서명/검증은 v1.1 스코프 외(D-50의 over-engineering 제외 원칙). |
| T-07P01-03 | Repudiation | MOCK-INDEX 편집 이력 | accept | git commit 메시지 + co-author 기록으로 충분. 자동 검증 스크립트는 v1.2 deferred. |
</threat_model>

<verification>
모든 파일 생성 후 run-once:
```bash
test -f .planning/templates/MOCK-LOG.md \
  && test -f .planning/MOCK-INDEX.md \
  && test -f .planning/README.md \
  && test -f .planning/phases/06-admin-backoffice/MOCK-LOG.md \
  && test -f docs/external-keys.md \
  && grep -q "docs/external-keys.md" .env.example \
  && echo "07-01 prep: all artifacts present"
```
</verification>

<success_criteria>
- INFRA-03 (5번 success criterion) 충족: `.planning/templates/MOCK-LOG.md`가 4필드(mocked path / reason / real-key re-verify step / target milestone) 표준으로 존재 + `.planning/phases/06-admin-backoffice/MOCK-LOG.md`가 3 entry pre-populate.
- INFRA-01 (4번 success criterion 부분) 충족: `docs/external-keys.md` 5분 체크리스트 존재 + `.env.example` 주석 link. (5분 완료 검증 자체는 `<manual-only>` per 07-VALIDATION.md — 이 plan의 자동 verify 범위 밖.)
- MOCK 정책 문서화 완료 — Phase 8 UAT-04가 동일 Phase 6 MOCK-LOG 파일에 항목을 append하면 된다.
</success_criteria>

<output>
After completion, create `.planning/phases/07-db-migration-apply-infra-foundation/07-01-SUMMARY.md` documenting:
- 5 files created + 1 file annotated
- 4 mandatory MOCK-LOG fields (D-50)
- docs/external-keys.md 6-step checklist (D-47 + D-48 no-screenshot rule)
- Exit state: ready for 07-02 apply plan (network gate).
</output>

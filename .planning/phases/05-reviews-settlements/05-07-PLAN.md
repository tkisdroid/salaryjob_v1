---
phase: 05-reviews-settlements
plan: 07
type: execute
wave: 6
depends_on: [05-05, 05-06]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md
  - .planning/phases/05-reviews-settlements/05-VERIFICATION.md
autonomous: false
gap_closure: false
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
  - SETL-01
  - SETL-02
  - SETL-03
  - DATA-05
user_setup: []

must_haves:
  truths:
    - "`npx vitest run` full suite exits 0 with at least Phase 4 baseline + ~15 new Phase 5 tests (4 reviews files × cases + 3 settlements files × cases + 1 exit file × 3 assertions) all GREEN"
    - "`NODE_ENV=production npx next build` completes with 0 errors and lists all Phase 5 new/rewritten pages (/my/applications/[id]/review, /biz/posts/[id]/applicants/[applicantId]/review, /my/settlements, /biz/settlements) as dynamic (ƒ)"
    - "`grep -rn --include='*.ts' --include='*.tsx' -E \"from ['\\\"](@/lib/mock-data|\\.\\.?/.*mock-data)['\\\"]\" src/ --exclude-dir=generated` returns exit code 1"
    - ".planning/STATE.md status flipped to milestone:v1 complete OR phase 5 marked as Completed; Performance Metrics updated with new plan count and test counts"
    - ".planning/REQUIREMENTS.md DATA-05 + REV-01..04 + SETL-01..03 all marked [x] Completed with commit hash references"
    - ".planning/ROADMAP.md Phase 5 row checked [x] with final plan count"
    - ".planning/phases/05-reviews-settlements/05-HUMAN-UAT.md exists with 3 manual scenarios (end-to-end loop under 1 min, mobile layout, review UX feel)"
    - ".planning/phases/05-reviews-settlements/05-VERIFICATION.md exists and documents per-requirement code evidence + test results (mirrors Phase 4's VERIFICATION.md structure)"
    - "Human checkpoint confirms: 탐색 → 지원 → 확정 → 체크인 → 체크아웃 → 리뷰 → 정산 확인 loop runs end-to-end against real Supabase in ≤ 1 minute"
  artifacts:
    - path: ".planning/phases/05-reviews-settlements/05-HUMAN-UAT.md"
      provides: "3 manual UAT scenarios for items that cannot be automated"
      min_lines: 50
    - path: ".planning/phases/05-reviews-settlements/05-VERIFICATION.md"
      provides: "Per-requirement code evidence + automated test results summary (Phase 4 pattern)"
      min_lines: 100
  key_links:
    - from: ".planning/STATE.md"
      to: "Phase 5 completion status"
      via: "direct markdown edit"
      pattern: "Phase 5.*Completed|v1.*Completed"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "8 Phase 5 requirement rows"
      via: "[ ] → [x] flip with commit ref"
      pattern: "REV-0[1-4].*Completed|SETL-0[1-3].*Completed|DATA-05.*Completed"
---

<objective>
Phase 5 final wave: full test suite verification + production build verification + Phase 5 VERIFICATION.md creation + HUMAN-UAT scenarios + STATE/REQUIREMENTS/ROADMAP updates + final human checkpoint on the complete Timee loop.

Purpose: Close out Phase 5 AND the v1 milestone with documented evidence. After this plan, the user should have a single verifiable answer to "is v1 done?": yes, because all 8 Phase 5 requirements are marked Completed with commit references and verified by automated + manual evidence.
Output: 2 new documentation files (05-HUMAN-UAT.md + 05-VERIFICATION.md) + 3 planning doc updates + final checkpoint.
</objective>

<execution_context>
@C:/Users/TG/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-reviews-settlements/05-VALIDATION.md
@.planning/phases/04-db/VERIFICATION.md
@.planning/phases/04-db/04-HUMAN-UAT.md
@.planning/phases/05-reviews-settlements/05-01-SUMMARY.md
@.planning/phases/05-reviews-settlements/05-02-SUMMARY.md
@.planning/phases/05-reviews-settlements/05-03-SUMMARY.md
@.planning/phases/05-reviews-settlements/05-04-SUMMARY.md
@.planning/phases/05-reviews-settlements/05-05-SUMMARY.md
@.planning/phases/05-reviews-settlements/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full verification gates (vitest + next build + grep) and write 05-VERIFICATION.md</name>
  <files>.planning/phases/05-reviews-settlements/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-db/VERIFICATION.md (template to mirror — per-requirement code evidence + test results)
    - .planning/phases/05-reviews-settlements/05-VALIDATION.md (Per-Task Verification Map with expected commands)
    - All 6 preceding plan SUMMARY.md files (cited above)
  </read_first>
  <behavior>
    - Run the 3 automated gates:
        1. `npx vitest run 2>&1 | tee /tmp/phase5-vitest.log` — capture full suite output
        2. `NODE_ENV=production npx next build 2>&1 | tee /tmp/phase5-build.log`
        3. `grep -rn --include='*.ts' --include='*.tsx' --include='*.js' --include='*.jsx' -E "from ['\"](@/lib/mock-data|\\.\\.?/.*mock-data)['\"]" src/ --exclude-dir=generated; echo "grep exit: $?"` — expect exit 1
    - Write .planning/phases/05-reviews-settlements/05-VERIFICATION.md mirroring the Phase 4 VERIFICATION.md structure:
        - Frontmatter: phase, verified date, status (passed / partial), gates (vitest counts, build counts, grep result), deferred (HUMAN-UAT scenarios)
        - Per-requirement section: REV-01 / REV-02 / REV-03 / REV-04 / SETL-01 / SETL-02 / SETL-03 / DATA-05 — each with file paths, key code excerpts (or line references), and test file that proves the behaviour
        - Migrations Verification: Plan 02 enum delta evidence (MCP SQL output)
        - Mock Removal Progress: Plan 06 exit gate evidence (grep output, file absence)
    - DO NOT fabricate numbers. If vitest shows 42 passing, write 42. If build shows 34 pages, write 34.
  </behavior>
  <action>
    1. Run the three verification commands, capturing full output:
       ```bash
       mkdir -p /tmp 2>/dev/null || true
       npx vitest run 2>&1 | tee /tmp/phase5-vitest.log
       NODE_ENV=production npx next build 2>&1 | tee /tmp/phase5-build.log
       grep -rn --include='*.ts' --include='*.tsx' --include='*.js' --include='*.jsx' \
         -E "from ['\"](@/lib/mock-data|\\.\\.?/.*mock-data)['\"]" \
         src/ --exclude-dir=generated
       echo "grep exit: $?"
       ```
    2. Extract key numbers:
       - From vitest: "Test Files" count, "Tests" count, "passing" count, "failing" count
       - From next build: total static pages, number of ƒ (dynamic) routes, any warnings
       - From grep: confirm exit 1 (no matches)
    3. Create .planning/phases/05-reviews-settlements/05-VERIFICATION.md with this shape:
       ```markdown
       ---
       phase: 05-reviews-settlements
       verified: <YYYY-MM-DDTHH:MM:SSZ>
       status: passed
       score: 8/8 must-haves verified (code-level) + 3 HUMAN-UAT deferred
       overrides_applied: 0
       verifier: gsd-verifier (goal-backward, code-reading)
       gates:
         vitest: <X files> / <Y passing> / <Z todo> / 0 failing
         next_build: <N> static pages, all Phase 5 routes dynamic
         mock_data_imports_in_src: 0 (grep exit code 1)
       deferred:
         - truth: "End-to-end loop completes under 1 minute on real Supabase"
           addressed_in: "HUMAN-UAT scenario 1"
           evidence: "Automated tests verify each link; full-loop stopwatch requires manual browser session."
         - truth: "Mobile 375px layout is readable and non-scrolling"
           addressed_in: "HUMAN-UAT scenario 2"
           evidence: "Component code uses Tailwind responsive classes; manual mobile viewport verification required."
         - truth: "Review UX feels Timee-aligned (star picker, chip grid, sticky submit)"
           addressed_in: "HUMAN-UAT scenario 3"
           evidence: "Subjective taste call on visual rhythm; cannot automate."
       ---

       # Phase 5 (리뷰·정산·목업 제거) Verification Report

       **Phase Goal**: Worker↔Business 양방향 리뷰 + 정산 실데이터 구동 + mock-data.ts 완전 제거

       **Verified**: <date>
       **Status**: PASS — 자동 검증 가능한 8개 목표 달성. 3개 UAT 시나리오는 수동 검증 대기.

       ---

       ## Verification Method

       1. 6개 plan의 SUMMARY.md 주장은 신뢰하지 않고 goal-backward 검증
       2. 각 REQ ID (REV-01..04, SETL-01..03, DATA-05)에 대해 실제 파일 라인 단위 읽음
       3. Stub / TODO / placeholder grep: 0건
       4. Key link wiring 확인 (Page → Action → DB → Response → UI)
       5. vitest 전체 suite 로컬 실행 (X초)
       6. next build (production) 로컬 실행
       7. `mock-data` import 전수 조사 (grep + tests/exit/mock-removal.test.ts)

       ---

       ## Per-Requirement Verification

       ### REV-01 — Worker → Business 리뷰 작성 (ACHIEVED)
       - **File**: `src/app/(worker)/my/applications/[id]/review/actions.ts`
       - `createWorkerReview` Server Action — Zod validate → requireApplicationOwner → gate status='settled' → $transaction { tx.review.create + $executeRaw UPDATE business_profiles + tx.application.update reviewGiven=true }
       - P2002 catch maps unique violation → already_reviewed
       - UI: `src/app/(worker)/my/applications/[id]/review/page.tsx` — Next 16 async params, requireApplicationOwner gate, getReviewByApplication pre-check, renders ReviewForm with WORKER_TO_BIZ_TAGS
       - Test: `tests/reviews/create-worker-to-biz.test.ts` — 3 cases PASS

       ### REV-02 — Business → Worker 리뷰 작성 (ACHIEVED)
       ... (similar structure)

       ### REV-03 — Application당 정확히 1회 uniqueness (ACHIEVED)
       - Schema-level `@@unique([applicationId, direction])` (existing from Phase 2) + P2002 catch in both review actions
       - Test: `tests/reviews/uniqueness.test.ts` — 2 cases PASS

       ### REV-04 — 리뷰 제출 시 rating/reviewCount 자동 업데이트 (ACHIEVED)
       - Atomic `$transaction` + `$executeRaw` with `ROUND((prev*count + new)::numeric / (count+1), 2)::numeric(3,2)` for Decimal(3,2) precision
       - WorkerProfile.reviewCount column added in Plan 05-03 (schema diff)
       - Test: `tests/reviews/aggregate.test.ts` — 3 cases PASS (count=0 edge, weighted math, concurrent)

       ### SETL-01 — pending → settled 전환 (ACHIEVED)
       - **File**: `src/app/(worker)/my/applications/[id]/check-in/actions.ts` line 212
       - Single literal change: `status: "completed"` → `status: "settled"` inside the existing atomic prisma.application.update (same transaction that writes earnings + actualHours + checkOutAt)
       - Plan 02 added `settled` enum value to prisma schema + Supabase
       - `DONE_STATUSES` in `src/lib/db/queries.ts` now includes both values for legacy compat
       - `adaptApplication` derives `settlementStatus` from status
       - Test: `tests/settlements/checkout-settled-transition.test.ts` — 2 cases PASS

       ### SETL-02 — Business 정산 히스토리 (ACHIEVED)
       - **File**: `src/lib/db/queries.ts` — `getBizSettlements` (findMany + JOIN on job.authorId) + `getBizSettlementTotals` ($queryRaw with AT TIME ZONE 'Asia/Seoul')
       - UI: `src/app/biz/settlements/page.tsx` REWRITE — Server Component with Promise.all totals + list, SettlementCard with side='biz'
       - Test: `tests/settlements/biz-history.test.ts` — 2 cases PASS

       ### SETL-03 — 총수입/이번 달 수입 실데이터 집계 (ACHIEVED)
       - **File**: `src/lib/db/queries.ts` — `getWorkerSettlementTotals` ($queryRaw with AT TIME ZONE 'Asia/Seoul' + date_trunc month boundary)
       - KST edge-case tests cover 2026-04-30 14:59 UTC (April KST) vs 15:00 UTC (May KST)
       - UI: `src/app/(worker)/my/settlements/page.tsx` REWRITE
       - Test: `tests/settlements/worker-aggregates.test.ts` — 3 cases PASS

       ### DATA-05 — src/lib/mock-data.ts 의존 0건 (ACHIEVED)
       - **Deletion**: file removed in Plan 05-06 Task 2
       - **Detachment**: prisma/seed.ts refactored to use prisma/seed-data.ts (or inlined), no imports from src/lib/mock-data
       - **Cleanup**: src/lib/types/job.ts + src/lib/job-utils.ts header comments scrubbed; Mock* backward-compat aliases removed
       - grep: `grep -rn --include='*.ts' --include='*.tsx' -E "from ['\"](@/lib/mock-data|\\.\\.?/.*mock-data)['\"]" src/ --exclude-dir=generated` returns exit 1
       - Test: `tests/exit/mock-removal.test.ts` — 3 assertions PASS (file absent, grep clean, seed detached)

       ---

       ## Migrations Verification

       | Migration | Verdict | Evidence |
       |---|---|---|
       | Plan 05-02 enum delta (ApplicationStatus += 'settled') | VERIFIED | MCP `SELECT unnest(enum_range(NULL::"ApplicationStatus"))` returns 7 values including 'settled' |
       | Plan 05-03 WorkerProfile.reviewCount column | VERIFIED | MCP information_schema.columns query returns 1 row for worker_profiles.reviewCount |

       ---

       ## Mock Removal Final Status (DATA-05 exit gate)

       ```bash
       $ grep -rn --include='*.ts' --include='*.tsx' --include='*.js' --include='*.jsx' \
           -E "from ['\"](@/lib/mock-data|\\.\\.?/.*mock-data)['\"]" \
           src/ --exclude-dir=generated
       $ echo $?
       1
       $ test -f src/lib/mock-data.ts; echo $?
       1
       ```

       ---

       ## Deferred (HUMAN-UAT)

       See `.planning/phases/05-reviews-settlements/05-HUMAN-UAT.md` for 3 manual scenarios.
       ```
    4. Run `git add .planning/phases/05-reviews-settlements/05-VERIFICATION.md`
    5. Commit: `docs(05-07): Phase 5 VERIFICATION.md — all 8 requirements PASS + gate evidence`
  </action>
  <verify>
    <automated>test -f .planning/phases/05-reviews-settlements/05-VERIFICATION.md && grep -q "REV-01 —" .planning/phases/05-reviews-settlements/05-VERIFICATION.md && grep -q "SETL-01 —" .planning/phases/05-reviews-settlements/05-VERIFICATION.md && grep -q "DATA-05 —" .planning/phases/05-reviews-settlements/05-VERIFICATION.md && npx vitest run 2>&1 | tail -5 | grep -E "passed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-reviews-settlements/05-VERIFICATION.md` exists and is at least 100 lines
    - Contains one section per REQ ID (REV-01, REV-02, REV-03, REV-04, SETL-01, SETL-02, SETL-03, DATA-05)
    - Frontmatter `status: passed` with concrete gate counts (not "TBD")
    - `npx vitest run 2>&1 | tail -10` output captured in the document shows 0 failures
    - `NODE_ENV=production npx next build 2>&1 | tail -10` output shows 0 errors
    - grep exit code 1 documented in the file
  </acceptance_criteria>
  <done>VERIFICATION.md captures the state of the project with per-requirement evidence and gate numbers from actual command output (not fabricated).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 05-HUMAN-UAT.md + update STATE/REQUIREMENTS/ROADMAP</name>
  <files>.planning/phases/05-reviews-settlements/05-HUMAN-UAT.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/04-db/04-HUMAN-UAT.md (template)
    - .planning/STATE.md (current phase position)
    - .planning/REQUIREMENTS.md (traceability table)
    - .planning/ROADMAP.md (Phase 5 section)
  </read_first>
  <behavior>
    - Create 05-HUMAN-UAT.md with 3 scenarios: (1) end-to-end loop under 1 minute, (2) mobile layout readability, (3) review UX feel subjective check.
    - Update STATE.md: bump progress to 32+/32+ plans, flip Phase 5 status, update Last Session Summary + Next Session Starting Point.
    - Update REQUIREMENTS.md: flip 8 rows ([ ] → [x]) with commit hash column. Also update the "Completed" count in the Coverage section.
    - Update ROADMAP.md: check [x] the Phase 5 row + Progress table row.
  </behavior>
  <action>
    1. Create .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md:
       ```markdown
       # Phase 5 HUMAN-UAT Scenarios

       **Phase**: 05-reviews-settlements
       **Created**: <date>
       **Prerequisite**: Phase 5 Plan 01-07 complete, `npm run dev` running, seeded dev DB with at least 3 settled applications for the test worker.

       ---

       ## Scenario 1 — End-to-end loop under 1 minute

       **Goal**: Verify ROADMAP success criterion #5: "탐색 → 지원 → 근무 → 리뷰 → 정산 확인 loop completes in under 1 minute with real Supabase round-trips."

       **Setup**:
       - Use the seeded worker account (e.g. worker@dev.gignow.com).
       - Use a business + job where time window gates allow immediate check-in (e.g. manipulate job.workDate + startTime to "now" via seed or admin SQL).
       - Have the biz side open in a second tab for QR token generation.

       **Steps** (stopwatch on):
       1. (0s) Navigate to `/home`. Browse list or map toggle.
       2. (5s) Open a job detail page, click "원탭 지원".
       3. (10s) Switch to biz tab, refresh applicants list, click "수락".
       4. (15s) Back to worker tab, navigate to `/my/applications/{id}/check-in`, click check-in (use fake GPS within geofence if necessary).
       5. (25s) Wait or fast-forward actualHours, then click "체크아웃" (requires QR scan from biz tab).
       6. (45s) On the worker's success screen, click "리뷰 작성". Fill 5 stars + 2 tags + short comment. Submit.
       7. (55s) Navigate to `/my/settlements`. Verify the just-completed shift appears in the list with correct earnings.
       8. Stop stopwatch.

       **Pass criteria**: Total elapsed time ≤ 60 seconds from step 1 to step 8. Every step responds within 1-2 seconds of the previous action.

       **Observed**: (fill in during UAT)
       **Verdict**: (pass/fail)

       ---

       ## Scenario 2 — Mobile 375px layout readability

       **Goal**: Verify REV-01/02 and SETL-02/03 UIs work on a mobile viewport without horizontal scroll or unreadable text.

       **Setup**: Chrome DevTools device toolbar, iPhone SE (375×667) preset.

       **Pages to verify**:
       1. `/my/settlements` — totals cards fit in 2-column grid; settlement cards don't overflow; review prompt banner is readable.
       2. `/my/applications/{id}/review` — star picker is large enough to tap; tag chips wrap to multiple rows; textarea and submit button are reachable without zoom.
       3. `/biz/settlements` — same checks for biz side.
       4. `/biz/posts/{jobId}/applicants/{applicantId}/review` — same as worker review page.

       **Pass criteria**:
       - Zero horizontal scroll on any page
       - All Korean text legible at 375px without zoom
       - All interactive elements (star buttons, chips, submit) have ≥ 44×44 pt touch targets
       - No layout shifts after initial paint

       **Observed**: (fill in)
       **Verdict**: (pass/fail)

       ---

       ## Scenario 3 — Review UX feel matches Timee / check-in flow rhythm

       **Goal**: Verify REV-01/02 UIs subjectively align with the Phase 4 check-in confirmation flow's "single-screen stepped" visual rhythm.

       **Compare**:
       - Side-by-side: `/my/applications/{id}/check-in` (Phase 4) vs `/my/applications/{id}/review` (Phase 5).

       **Checkpoints**:
       - Both pages have a prominent single-purpose heading at the top
       - Both pages use sections with small bold labels above the interactive elements
       - Both pages have a sticky-at-bottom action button
       - Both pages use consistent spacing (gap-6 between sections) and card-free flat layout for the main form
       - Korean microcopy (section labels, button text) matches the project voice (polite, concise, no unnecessary explanations)

       **Pass criteria**: Subjective but verifiable by inspection — the review page "feels like" a continuation of the check-in page, not a different app.

       **Observed**: (fill in)
       **Verdict**: (pass/fail)

       ---

       ## Sign-off

       - [ ] Scenario 1: end-to-end loop under 1 minute
       - [ ] Scenario 2: mobile 375px layout
       - [ ] Scenario 3: review UX rhythm match

       When all 3 pass, Phase 5 is complete at the UAT level as well as the automated-gate level. Update STATE.md milestone status to v1-complete.
       ```
    2. Update .planning/STATE.md:
       - Frontmatter: `progress.total_plans` += 7 (Phase 5 plan count)
       - Current Position: set "Phase: 05 (reviews-settlements) — COMPLETED" if manual UAT passes, else "Phase: 05 — code complete, HUMAN-UAT pending"
       - Phase Progress table: Phase 5 row status → "Completed <date>"
       - Accumulated Context > Key Decisions: append a row for Plan 05-02's WorkerProfile.reviewCount addition (it was a discovered dependency not in research)
       - Open TODOs: remove "Phase 5 계획 수립" (now complete), add "Phase 5 HUMAN-UAT 3 시나리오 수동 검증" if not yet done
       - Blockers: none if all automated gates passed; otherwise list the specific failure
       - Last Session Summary: summarize Plans 01-07 with commit refs
       - Next Session Starting Point: "v1 milestone complete — kickoff v2 planning" OR the HUMAN-UAT checklist
    3. Update .planning/REQUIREMENTS.md Traceability table:
       - Find the rows for DATA-05, REV-01, REV-02, REV-03, REV-04, SETL-01, SETL-02, SETL-03
       - Update each status column from "Pending" / "Phase 5 Pending (exit criterion)" to "Completed <date> (commit `<hash>`)"
       - Update Coverage summary: Completed count "35/43" → "43/43 ✓ (all v1 requirements)"
       - Update the note line about remaining requirements
       - Flip the `- [ ]` checkbox to `- [x]` in the v1 Requirements section for all 8 IDs
    4. Update .planning/ROADMAP.md:
       - Phase 5 row in the "Phases" list: `- [ ]` → `- [x]` with date + completion commit
       - Phase 5 section heading: add "(완료 <date>)" suffix
       - Plans field: update count from "TBD" to the actual final count (7)
       - Plan list: insert all 7 Phase 5 PLAN paths with brief one-line objectives
       - Progress table: Phase 5 row from "0/? | Not started | -" to "7/7 | Completed | <date>"
       - If the user agrees v1 is done at the UAT gate: add an "**Exit Milestone: v1 COMPLETE**" banner at the top of the Phase 5 section
    5. Run `git add .planning/` and commit: `docs(05-07): Phase 5 complete — STATE/REQUIREMENTS/ROADMAP + HUMAN-UAT scenarios`
  </action>
  <verify>
    <automated>test -f .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md && grep -q "Scenario 1 — End-to-end loop" .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md && grep -q "REV-01.*Completed\|REV-01.*Phase 5.*Completed" .planning/REQUIREMENTS.md && grep -q "DATA-05.*Completed" .planning/REQUIREMENTS.md && grep -q "Phase 5.*Completed\|\[x\].*Phase 5" .planning/ROADMAP.md
    <automated>test -f .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md && grep -q "Scenario 1" .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md && grep -q "DATA-05" .planning/REQUIREMENTS.md && grep -q "Phase 5" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-reviews-settlements/05-HUMAN-UAT.md` exists with 3 scenario headings (Scenario 1, 2, 3) and Pass criteria + Observed + Verdict fields per scenario
    - `.planning/STATE.md` Phase Progress table row 5 shows Status "Completed" with a date (or "Code complete — HUMAN-UAT pending")
    - `.planning/STATE.md` Open TODOs no longer contains the literal "Phase 5 계획 수립"
    - `.planning/REQUIREMENTS.md` Traceability table has 8 rows containing "Phase 5" that read "Completed <date>" (not "Pending")
    - `.planning/REQUIREMENTS.md` Coverage section says "43/43" completed (up from 35/43)
    - `.planning/REQUIREMENTS.md` v1 Requirements section has `- [x]` for DATA-05, REV-01..04, SETL-01..03 (8 flipped checkboxes)
    - `.planning/ROADMAP.md` Phase 5 entry in the Phases list starts with `- [x]`
    - `.planning/ROADMAP.md` Progress table row 5 shows "7/7 | Completed"
    - git log shows a single `docs(05-07): Phase 5 complete` commit touching only the 4 documentation files
  </acceptance_criteria>
  <done>All Phase 5 planning artefacts carry the completion state. v1 milestone traceability is ready for the final human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: FINAL HUMAN VERIFY — Phase 5 + v1 milestone sign-off</name>
  <what-built>
    Phase 5 is automated-complete (plans 01-06) and documented (plan 07 tasks 1-2). The only remaining work is a single human pass over the 3 HUMAN-UAT scenarios in `.planning/phases/05-reviews-settlements/05-HUMAN-UAT.md` plus a final confirmation that the v1 milestone as a whole delivers on the Timee promise: 면접 제로 · 탭 하나 확정 · 근무 후 즉시 정산 · mock-data 완전 제거.
  </what-built>
  <how-to-verify>
    Prerequisites:
    - `npm run dev` running (or a deployed dev URL)
    - Seeded Supabase with the Phase 5-updated seed (past-job fixtures now use 'settled' status)
    - Browser with Chrome DevTools for mobile viewport testing

    Execute the 3 scenarios in `.planning/phases/05-reviews-settlements/05-HUMAN-UAT.md`:

    1. **Scenario 1 — End-to-end loop under 1 minute**: run the full 탐색→지원→확정→체크인→체크아웃→리뷰→정산 loop with a stopwatch. Record the total elapsed time. Pass if ≤60 seconds.

    2. **Scenario 2 — Mobile 375px layout**: open all 4 Phase 5 pages in Chrome DevTools iPhone SE viewport. Verify no horizontal scroll, all Korean text legible, tap targets ≥44pt.

    3. **Scenario 3 — Review UX rhythm match**: visually compare `/my/applications/{id}/check-in` (Phase 4) with `/my/applications/{id}/review` (Phase 5). Confirm both feel like part of the same app.

    After running all three, fill in the Observed + Verdict fields in 05-HUMAN-UAT.md and commit: `docs(05-07): HUMAN-UAT results for Phase 5 + v1 milestone sign-off`

    Finally, confirm the v1 milestone promise:
    - Worker can explore jobs without an account, apply with one tap when logged in, work, and see earnings instantly — NO resume, NO interview, NO delayed settlement
    - Business can post jobs, accept/reject applicants, generate checkout QR, and see settlement history
    - Both sides can write bilateral reviews with atomic rating aggregation
    - The codebase contains ZERO references to src/lib/mock-data.ts
    - Full test suite + production build are GREEN

    Report back with:
    (a) UAT scenario results (pass/fail per scenario + observations)
    (b) v1 promise confirmation (approved / not approved)
    (c) Any follow-up items to carry into v2 planning

    If any scenario fails, describe the specific observation so the planner can schedule a gap-closure plan. Small cosmetic issues (copy tweaks, spacing) can be deferred to a lightweight post-v1 polish pass if you choose.
  </how-to-verify>
  <files>N/A — human checkpoint, no file modifications by the executor agent.</files>
  <action>HUMAN TASK: Execute the 3 scenarios in .planning/phases/05-reviews-settlements/05-HUMAN-UAT.md. Fill in Observed + Verdict fields. Confirm v1 milestone promise. Do NOT proceed to v2 planning until sign-off.</action>
  <verify>Human reports back with UAT scenario results + v1 promise confirmation.</verify>
  <done>All 3 UAT scenarios pass + user types "approved — v1 complete".</done>
  <resume-signal>
    Type "approved — v1 complete" to close out Phase 5 and the v1 milestone.
    OR: describe specific UAT failures for a gap-closure plan.
    OR: "approved with polish TODO" and list the small items to defer.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation ↔ reality | VERIFICATION.md assertions are goal-backward-verified via actual command output (vitest/build/grep). Plan 07 Task 1 MUST capture real output, not guesses. |
| planning docs ↔ execution state | STATE/REQUIREMENTS/ROADMAP updates must reference real commit hashes after each preceding plan's commit has landed. No placeholder "TBD" allowed in the final state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | Tampering | Fabricated verification numbers in 05-VERIFICATION.md | mitigate | Task 1 instructs the executor to `tee` command output to /tmp logs and copy the actual lines into the document. Reviewer can re-run the same commands at any time for cross-check. |
| T-05-26 | Information Disclosure | Commit hashes in REQUIREMENTS.md traceability | accept | Public repo; commit hashes are not sensitive. |
| T-05-27 | Denial of Service | Human checkpoint blocks v1 shipment | accept | Only 3 UAT scenarios, ~15 minutes of manual work. Pre-approved by project policy (CLAUDE.md memory: "작업 완료 시 GitHub 자동 푸시"). |
</threat_model>

<verification>
- `npx vitest run 2>&1 | tail -10` reports 0 failures and a count strictly greater than Phase 4 baseline (109 tests)
- `NODE_ENV=production npx next build 2>&1 | tail -10` reports 0 errors; Phase 5 dynamic routes listed
- Plan 07 grep exit-gate command returns exit code 1
- `.planning/phases/05-reviews-settlements/05-VERIFICATION.md` exists with 8 per-REQ sections and real gate numbers
- `.planning/phases/05-reviews-settlements/05-HUMAN-UAT.md` exists with 3 scenarios
- STATE/REQUIREMENTS/ROADMAP updates committed
- Human checkpoint returns approval
</verification>

<success_criteria>
Phase 5 is complete at the automated gate AND documented AND manually verified. v1 milestone closes with 43/43 requirements delivered. Project is ready for v2 planning (AI matching, Toss payments, native notifications, etc.) or ship-to-users.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reviews-settlements/05-07-SUMMARY.md` documenting:
- Plan 07 Task 1 gate evidence (vitest/build/grep actual output excerpts)
- Plan 07 Task 2 doc updates diff summary
- Plan 07 Task 3 UAT scenario results (filled in by the human)
- Final v1 milestone status
- Cross-links to all 7 Phase 5 SUMMARY files
</output>

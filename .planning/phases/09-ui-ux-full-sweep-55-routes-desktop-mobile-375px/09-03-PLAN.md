---
phase: 09-ui-ux-full-sweep-55-routes-desktop-mobile-375px
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - .review/phase9-issues.json
  - .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md
  - .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [QA-01, QA-02, QA-03, QA-04, QA-05]

must_haves:
  truths:
    # Maps to ROADMAP Phase 9 Success Criteria #1-#5 — Plan 03 is the ONLY plan that closes them.
    - "Human reviewer has read every critical + high entry in `.review/phase9-issues.json` and either authored a fix commit (and populated `fixCommit: <sha>`) OR documented MOCK-LOG justification for deferred entries (QA-04)"
    - "`npx tsx scripts/review/phase9-issue-writer.ts --verify` exits 0 — meaning 0 critical + 0 high without `fixCommit`"
    - "`09-REVIEW.md` frontmatter contains `phase9_complete: true`, `critical_issues: 0`, `high_issues: 0` on the final iteration"
    - "Human has spot-checked 3 random routes per persona (worker + biz + admin = 9 routes total) in headed mode and recorded verdicts in 09-REVIEW.md"
    - "All 5 ROADMAP Phase 9 Success Criteria attested by human reviewer in 09-REVIEW.md appendix — not pending"
    - "`.planning/REQUIREMENTS.md` traceability rows QA-01..QA-05 flipped from `pending` to `complete (YYYY-MM-DD, commit <sha>)` only after human sign-off"
    - "`.planning/STATE.md` Phase Progress row shows Phase 9 = Complete"
    - "No invocation of `scripts/review/auto-fix-loop.ts` on Phase 9 findings (Pitfall 4 enforced through closure)"
  artifacts:
    - path: ".review/phase9-issues.json"
      provides: "Aggregated Phase9Issue[]; humans populate fixCommit for each critical/high entry"
      contains: "fixCommit"
    - path: ".planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md"
      provides: "Final review report with phase9_complete frontmatter + issue register + 5-SC attestation"
      contains: "phase9_complete: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QA-01..QA-05 rows updated to complete + commit SHA + date"
      contains: "QA-04"
    - path: ".planning/STATE.md"
      provides: "Phase Progress table row for Phase 9 = Complete"
      contains: "9. UI/UX Full Sweep"
    - path: ".planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md"
      provides: "Rows 09-03-01 + 09-03-02 flipped to green + frontmatter nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".review/phase9-issues.json"
      to: ".review/phase9-*.json shards"
      via: "phase9-issue-writer --aggregate merges Plan 02 shards into unified file"
      pattern: "phase9-issues\\.json"
    - from: ".planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md"
      to: "scripts/review/phase9-report-writer.ts"
      via: "report-writer reads .review/phase9-issues.json, emits 09-REVIEW.md with frontmatter"
      pattern: "phase9_complete"
    - from: "09-REVIEW.md"
      to: "fix commits on the feature branch"
      via: "each critical/high issue has fixCommit: <sha>; git log shows fix(09): commits matching issue IDs"
      pattern: "fix\\(09\\)"
---

<objective>
Wave 3: aggregate the Plan 02 shards, surface critical/high issues to a human reviewer, receive human-authored fix commits with `fixCommit: <sha>` back-references, re-run the sweep until `.review/phase9-issues.json` has 0 critical + 0 high without fixCommit, emit final `09-REVIEW.md` with `phase9_complete: true`, attest the 5 ROADMAP Success Criteria via human spot-check, and flip QA-01..QA-05 to `complete` in REQUIREMENTS.md + STATE.md.

This plan is checkpointed (`autonomous: false`) because QA-04 requires human-authored fix commits per RESEARCH.md Pitfall 4 ("Phase 9 findings are semantic — design decisions — and MUST be human-authored"). Auto-fix loop is banned here.

Deviation from orchestrator spec: the canonical frontmatter field is `phase9_complete` (not `production_ready` as Phase 07.1 uses). This matches Plan 01 + Plan 02 artifact contracts AND RESEARCH.md §Open Questions #1 recommendation ("Phase 9 doesn't have a 'gates' concept — it has 'checklist buckets + severity counts.' Frontmatter: `phase9_complete: true|false`").

Output: `.review/phase9-issues.json` with every critical/high linked to fixCommit; `09-REVIEW.md` with `phase9_complete: true`; REQUIREMENTS.md QA-01..QA-05 = complete; STATE.md Phase 9 = Complete; 09-VALIDATION.md rows 09-03-01 + 09-03-02 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-RESEARCH.md
@.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md
@.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-01-PLAN.md
@.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-02-PLAN.md
@AGENTS.md
@CLAUDE.md
@scripts/review/phase9-issue-writer.ts
@scripts/review/phase9-report-writer.ts
@tests/review/phase9/checklist-base.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-01a: Run full Phase 9 sweep + aggregate shards</name>
  <read_first>
    - `scripts/review/phase9-issue-writer.ts` (Plan 01 Task 01-03) — `--aggregate` reads `.review/phase9-*.json` shards, writes `.review/phase9-issues.json`
    - `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-01-PLAN.md` — npm scripts `review:phase9`, `review:phase9:tokens`, `review:phase9:aggregate`, `review:phase9:verify`, `review:phase9:report`
    - `tests/review/phase9/worker-checklist.spec.ts` (Plan 02 Task 02-01) — emits `.review/phase9-worker-{project}.json`
    - `tests/review/phase9/biz-checklist.spec.ts` (Plan 02 Task 02-02) — emits `.review/phase9-biz-{project}.json`
    - `tests/review/phase9/admin-checklist.spec.ts` (Plan 02 Task 02-02) — emits `.review/phase9-admin-{project}.json`
    - `tests/review/phase9/empty-state.spec.ts` (Plan 02 Task 02-03) — emits `.review/phase9-empty-{project}.json`
    - `tests/review/phase9/tab-bar-occlusion.spec.ts` (Plan 02 Task 02-04) — emits `.review/phase9-occlusion-mobile-375.json`
    - `tests/review/phase9/token-drift.test.ts` (Plan 02 Task 02-05) — emits `.review/phase9-token-drift.json` via vitest
  </read_first>
  <files>
    .review/phase9-issues.json
  </files>
  <action>
    Mechanical execution — no code authoring, just run commands and verify artifacts.

    **Preconditions — verify before starting:**
    - Docker Desktop is running (Supabase local-stack prerequisite)
    - `.env.test` exists and points to local Supabase (NOT production DATABASE_URL)
    - Plan 01 + Plan 02 committed and green on `npx tsc --noEmit`

    **Execution sequence — run IN THIS ORDER:**

    ```bash
    # 1. Boot local Supabase stack (reuse if already running)
    npm run review:stack

    # 2. Apply Phase 07.1 populated seed (3+3+1+10+5+2+3+1 records)
    npm run review:seed

    # 3. Run all 5 Playwright specs under both viewport projects (2 projects × 5 specs = 10 runs)
    REVIEW_RUN=1 npm run review:phase9
    # Expected side-effect (shards written regardless of test pass/fail — see Plan 02 test.afterAll):
    #   .review/phase9-worker-review-desktop.json
    #   .review/phase9-worker-mobile-375.json
    #   .review/phase9-biz-review-desktop.json
    #   .review/phase9-biz-mobile-375.json
    #   .review/phase9-admin-review-desktop.json
    #   .review/phase9-admin-mobile-375.json
    #   .review/phase9-empty-review-desktop.json
    #   .review/phase9-empty-mobile-375.json
    #   .review/phase9-occlusion-mobile-375.json

    # 4. Run vitest static-analysis — writes .review/phase9-token-drift.json
    npx vitest run tests/review/phase9/token-drift.test.ts

    # 5. Aggregate all shards into .review/phase9-issues.json
    npm run review:phase9:aggregate
    # Expected stdout: [phase9-issue-writer] aggregated <N> issue(s): {"critical":<c>,"high":<h>,"medium":<m>,"low":<l>,"info":<i>}

    # 6. Write initial 09-REVIEW.md (iteration 1) — phase9_complete will be false if C/H > 0
    npx tsx scripts/review/phase9-report-writer.ts --iter=1 || true
    # --iter=1 = first run. `|| true` allows exit 1 (phase9_complete=false) without halting — that's expected at this point.
    ```

    **Known caveats — do NOT treat as fatal:**
    - `supabase start` reporting "already running" is fine — reuse the existing stack.
    - `npm run review:phase9` exiting non-zero because test assertions fail IS expected here — shards are written inside `test.afterAll` regardless. Only treat as fatal if (a) NO shard files exist, (b) `npx tsc --noEmit` is broken, or (c) the Playwright webServer fails to start.
    - Runtime estimate: ~20-30 minutes for the full sweep on a warm stack, matching Phase 07.1 D-17 budget (~30 min target).

    **Single artifact produced:** `.review/phase9-issues.json` — valid JSON, top-level array; may be `[]` (ideal) or contain many entries across all severities (typical first run).
  </action>
  <verify>
    <automated>
      test -f .review/phase9-issues.json &&
      node -e "const f=require('./.review/phase9-issues.json');if(!Array.isArray(f))process.exit(1);console.log('aggregate OK, '+f.length+' issues')" &&
      test -f .review/phase9-token-drift.json &&
      npx tsx scripts/review/phase9-issue-writer.ts --stats
    </automated>
  </verify>
  <acceptance_criteria>
    - `.review/phase9-issues.json` exists and parses as a top-level JSON array
    - `npx tsx scripts/review/phase9-issue-writer.ts --stats` exits 0 and prints a single JSON line with keys `critical`, `high`, `medium`, `low`, `info`, `total`
    - At least 4 shard files exist under `.review/phase9-*.json` (even if empty arrays — proves Plan 02 specs executed `test.afterAll`)
    - `.review/phase9-token-drift.json` exists (proves Plan 02 Task 02-05 vitest test ran)
    - No invocation of `scripts/review/auto-fix-loop.ts` in this task's commands (Pitfall 4)
    - `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md` row 09-03-01 updated to record the aggregate `total` count and timestamp
  </acceptance_criteria>
  <done>Sweep complete; aggregate produced. Task 03-01b checkpoint can surface critical + high to the human reviewer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-01b: Human fix loop for critical + high issues (QA-04)</name>
  <what-built>
    Plan 02 specs produced `.review/phase9-*.json` shards; Task 03-01a aggregated them into `.review/phase9-issues.json`. This checkpoint surfaces critical + high severity entries to the human reviewer for one-by-one triage: fix, defer, or reject.

    Each Phase9Issue has shape `{id, severity, route, viewport, persona, bucket, message, evidence, fixCommit?}`. Critical/high entries lacking `fixCommit` BLOCK phase close per QA-04 ("모든 critical·high 등급 이슈는 close 전에 수정되어 fix 커밋이 이슈 항목에 링크되어 있고").
  </what-built>
  <how-to-verify>
    Human reviewer, execute this loop until `npx tsx scripts/review/phase9-issue-writer.ts --verify` exits 0.

    **Step A — Inspect blockers:**
    ```bash
    # Count summary
    npx tsx scripts/review/phase9-issue-writer.ts --stats

    # Filter to critical + high (pretty-print)
    node -e "const f=require('./.review/phase9-issues.json');const blockers=f.filter(i=>i.severity==='critical'||i.severity==='high');console.log(JSON.stringify(blockers,null,2))" > .review/phase9-blockers.json
    cat .review/phase9-blockers.json | head -200
    ```

    **Step B — For EACH critical + high issue, decide ONE outcome:**

    1. **FIX (default for real issues):**
       - Read `message` + `evidence` fields
       - Open the implicated route in a browser to reproduce
       - Author a fix in `src/...` — typical patches are Tailwind className changes, empty-state Korean copy additions, CTA consolidation, or `pb-20` padding for tab-bar-occlusion
       - Commit using convention: `fix(09): <issue-slug> [qa-NN]` — e.g., `fix(09): consolidate duplicate '지원하기' CTA on posts detail [qa-05]`
       - Capture short SHA: `git log -1 --format=%h`
       - Populate the issue's `fixCommit` field:
         ```bash
         node -e "
           const fs=require('fs');
           const p='.review/phase9-issues.json';
           const arr=JSON.parse(fs.readFileSync(p,'utf8'));
           const idx=arr.findIndex(i=>i.id==='<ISSUE_ID>');
           if(idx<0){console.error('not found');process.exit(1)}
           arr[idx].fixCommit='<SHORT_SHA>';
           fs.writeFileSync(p,JSON.stringify(arr,null,2));
         "
         ```

    2. **DEFER (rare — v1.2 work or external-key blocked):**
       - Downgrade `severity` from critical/high to `medium` with justification in the `message` field
       - Populate `fixCommit: "deferred-v1.2-<reason-slug>"` — synthetic marker satisfying `--verify` gate without a real commit
       - Add entry to `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/MOCK-LOG.md` using the milestone-wide template: `mocked path / reason / real-key re-verify step / target milestone`

    3. **REJECT (very rare — harness false positives):**
       - Document in 09-REVIEW.md appendix "False Positive" section with reason
       - Downgrade to `info` (then it doesn't block `--verify`)
       - Update `tests/review/phase9/config/token-allowlist.json` OR `empty-state-map.json` (whichever bucket) so the false positive doesn't recur next run

    **Step C — Re-run the sweep (after ALL critical + high addressed):**
    ```bash
    REVIEW_RUN=1 npm run review:phase9
    npx vitest run tests/review/phase9/token-drift.test.ts
    npm run review:phase9:aggregate
    npx tsx scripts/review/phase9-issue-writer.ts --verify
    # --verify exits 0 only when (critical + high WITHOUT fixCommit) === 0
    ```

    If `--verify` exits non-zero, the stdout `reason` lists unfixed issue IDs. Loop back to Step B for those.

    **Estimated human effort:** highly variable. If Plan 02 surfaces 0-5 blockers, budget ~30 minutes per blocker (read, reproduce, fix, commit, link). If 15+ blockers, budget 1-2 days and consider scope reduction: downgrade "high" to "medium" with MOCK-LOG entries for legitimate deferrals.

    **Pitfall 4 reminder:** do NOT invoke `scripts/review/auto-fix-loop.ts` on these findings. Every fix MUST be human-authored.
  </how-to-verify>
  <files>
    .review/phase9-issues.json
    src/**/*.tsx (edited by human during fix commits)
  </files>
  <action>
    See <how-to-verify> above. Checkpoint executed by human reviewer who reads .review/phase9-issues.json, addresses every critical/high entry via FIX/DEFER/REJECT, populates fixCommit field, and re-runs the sweep until phase9-issue-writer.ts --verify exits 0. No autonomous action — Pitfall 4 bans auto-fix invocation on Phase 9 findings.
  </action>
  <verify>
    <automated>npx tsx scripts/review/phase9-issue-writer.ts --verify</automated>
  </verify>
  <done>Resume-signal recorded: all-C-H-resolved (verify exits 0) OR paused/stop with justification in STATE.md.</done>
  <resume-signal>
    Resume with ONE of:
    - `all-C-H-resolved` — `npx tsx scripts/review/phase9-issue-writer.ts --verify` exits 0 (ready for Task 03-02)
    - `paused: <reason>` — human reviewer is stopping before completing the loop (end of session, etc.). Include description of remaining work.
    - `stop: <reason>` — Phase 9 cannot close in this session. STATE.md must be updated with blocker note before session end.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 03-02: --verify gate + 09-REVIEW.md with phase9_complete: true + REQUIREMENTS in-progress flip</name>
  <read_first>
    - `scripts/review/phase9-issue-writer.ts` (Plan 01) — `--verify` flag semantics
    - `scripts/review/phase9-report-writer.ts` (Plan 01) — emits 09-REVIEW.md frontmatter
    - `.review/phase9-issues.json` — aggregated after Task 03-01b resume-signal `all-C-H-resolved`
    - `.planning/REQUIREMENTS.md` lines 84-116 — Traceability table where QA-01..QA-05 rows live
    - `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md` — frontmatter + per-task rows table
  </read_first>
  <files>
    .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md
    .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Mechanical closure steps — no human judgment; Task 03-01b already surfaced + resolved all critical/high.

    **Step 1: `--verify` gate — MUST exit 0:**
    ```bash
    npx tsx scripts/review/phase9-issue-writer.ts --verify
    # Expected stdout: {"ok":true,"counts":{"critical":0,"high":0,"medium":<n>,"low":<n>,"info":<n>}}
    # Expected exit: 0
    ```

    If exit code ≠ 0, Task 03-01b is NOT actually complete — return to Task 03-01b. DO NOT proceed until `--verify` exits 0.

    **Step 2: Emit final 09-REVIEW.md with `phase9_complete: true`:**
    ```bash
    # Use iteration number reflecting how many sweep passes happened in Task 03-01b. Typical=2.
    npx tsx scripts/review/phase9-report-writer.ts --iter=<N>
    # Expected stdout: {"complete":true}
    # Expected exit: 0
    # Expected side-effect: .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md
    #   frontmatter phase9_complete: true, critical_issues: 0, high_issues: 0
    ```

    **Step 3: Verify 09-REVIEW.md frontmatter via grep:**
    ```bash
    head -20 .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md | grep -E "^phase9_complete: true" || exit 1
    head -20 .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md | grep -E "^critical_issues: 0" || exit 1
    head -20 .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md | grep -E "^high_issues: 0" || exit 1
    head -20 .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md | grep -E "^routes_swept: 54" || exit 1
    ```

    **Step 4: Append Success Criteria attestation appendix to 09-REVIEW.md** using the Edit tool. Append this block to the END of the file (after the issue table the report-writer emitted). Replace `<TIMESTAMP>` with `new Date().toISOString()` result, `<REVIEWER>` with `git config user.name`:

    ```markdown

    ## ROADMAP Phase 9 — 5 Success Criteria Attestation

    Populated by human reviewer in Task 03-03 sign-off. Do NOT mark PASS until visual spot-check is complete.

    | # | Success Criterion | Status | Evidence |
    |---|-------------------|--------|----------|
    | 1 | Worker 전 라우트 (home, search, job detail, apply, my/schedule, my/applications, my/shifts, reviews, settlements, profile, auth 흐름) Desktop + Mobile 375px 체크리스트 전 항목 PASS | pending | See issue table above — worker rows |
    | 2 | Business 전 라우트 (dashboard, jobs CRUD, applications inbox, shifts monitoring, reviews, profile, biz verify, settlements) Desktop + Mobile 체크리스트 전 항목 PASS | pending | Biz rows |
    | 3 | Admin 전 라우트 (/admin dashboard, /admin/businesses list + detail, commission edit, 등록증 열람) Desktop + Mobile 체크리스트 전 항목 PASS | pending | Admin rows |
    | 4 | 모든 critical/high 이슈 fix 커밋 링크 완료 (low/info 백로그 이월 가능) | pending → auto-verified via --verify | `fix_commits_linked` in frontmatter |
    | 5 | shadcn 디자인 토큰 드리프트, 동일 액션 중복 버튼, 모바일 하단 탭바로 가려진 CTA = 0건 | pending | token-drift + button-dup + tab-bar-occlusion buckets |

    ## Visual Spot-Check (Task 03-03)

    Pending Task 03-03 completion. 3 random routes per persona (worker/biz/admin = 9 routes) opened in headed mode, confirmed substantive render.

    - Worker routes spot-checked: `<LIST_AFTER_03-03>`
    - Biz routes spot-checked: `<LIST_AFTER_03-03>`
    - Admin routes spot-checked: `<LIST_AFTER_03-03>`
    - Reviewer: `<REVIEWER>`
    - Timestamp: `<TIMESTAMP>`
    ```

    **Step 5: Flip `.planning/REQUIREMENTS.md` QA-01..QA-05 rows from `pending` to `in-progress`** (final `complete` flip happens in Task 03-03):

    Edit the `## Traceability` section; replace:
    ```
    | QA-01  | Phase 9 | TBD | pending |
    | QA-02  | Phase 9 | TBD | pending |
    | QA-03  | Phase 9 | TBD | pending |
    | QA-04  | Phase 9 | TBD | pending |
    | QA-05  | Phase 9 | TBD | pending |
    ```
    with:
    ```
    | QA-01  | Phase 9 | 09-02 | in-progress (09-REVIEW.md phase9_complete=true; awaiting 03-03 sign-off) |
    | QA-02  | Phase 9 | 09-02 | in-progress (09-REVIEW.md phase9_complete=true; awaiting 03-03 sign-off) |
    | QA-03  | Phase 9 | 09-02 | in-progress (09-REVIEW.md phase9_complete=true; awaiting 03-03 sign-off) |
    | QA-04  | Phase 9 | 09-03 | in-progress (all C/H have fixCommit; --verify exit 0) |
    | QA-05  | Phase 9 | 09-02 | in-progress (token-drift + tab-bar-occlusion specs both green) |
    ```

    **Step 6: Flip 09-VALIDATION.md:** Edit `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md` — change rows 09-03-01 and 09-03-02 status from ⬜ pending to ✅ green with timestamp; flip frontmatter `nyquist_compliant: false` → `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>
      npx tsx scripts/review/phase9-issue-writer.ts --verify &&
      test -f .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &&
      grep -q "^phase9_complete: true" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &&
      grep -q "^critical_issues: 0" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &&
      grep -q "^high_issues: 0" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &&
      grep -q "^routes_swept: 54" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &&
      grep -q "5 Success Criteria Attestation" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &&
      grep -q "| QA-01  | Phase 9 | 09-02 | in-progress" .planning/REQUIREMENTS.md &&
      grep -q "| QA-04  | Phase 9 | 09-03 | in-progress" .planning/REQUIREMENTS.md &&
      grep -q "nyquist_compliant: true" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/review/phase9-issue-writer.ts --verify` exits 0 (MANDATORY precondition)
    - `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md` exists
    - Frontmatter contains each of these lines verbatim (grep each): `phase9_complete: true`, `critical_issues: 0`, `high_issues: 0`, `routes_swept: 54`
    - Frontmatter also contains `viewports: [review-desktop, mobile-375]` (grep literal)
    - 09-REVIEW.md body contains literal string "5 Success Criteria Attestation"
    - REQUIREMENTS.md rows QA-01..QA-05 show `in-progress` status with `09-02` or `09-03` Plan ID references (pending removed)
    - 09-VALIDATION.md frontmatter contains `nyquist_compliant: true` (replacing earlier `false`)
    - No invocation of `scripts/review/auto-fix-loop.ts` in this task's commands
    - No edits to `src/**`, `prisma/**`, `supabase/migrations/**`, `playwright.config.ts`, `package.json`, Phase 07.1 primitives
  </acceptance_criteria>
  <done>Mechanical closure complete. `phase9_complete: true` written. REQUIREMENTS.md rows flipped to in-progress. Ready for Task 03-03 human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-03: Human sign-off — 5 Success Criteria attestation + 9-route visual spot-check + REQUIREMENTS complete flip</name>
  <what-built>
    Task 03-02 emitted `09-REVIEW.md` with `phase9_complete: true` based on automated gates. This checkpoint is the final human eye: confirm the 5 ROADMAP Phase 9 Success Criteria are ACTUALLY satisfied (not just that the numbers look right) and catch any "looks wrong" case the automation missed via a 9-route visual spot-check.
  </what-built>
  <how-to-verify>
    Human reviewer, perform this sign-off protocol:

    **Part A — Attest the 5 Success Criteria in 09-REVIEW.md:**

    Open `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md`. For each attestation row, change `pending` to `PASS` (or `FAIL` with reason) and add an evidence link/reference:

    1. **SC-1 (Worker routes all buckets):** Review the issue table rows where `persona=worker`. If every entry is `medium`/`low`/`info` OR has a fixCommit, mark PASS.
    2. **SC-2 (Business routes all buckets):** Same check for `persona=biz`.
    3. **SC-3 (Admin routes all buckets):** Same check for `persona=admin`.
    4. **SC-4 (All C/H have linked fix commits):** Run `git log --grep="fix(09):"` — count matching commits. Compare to `fix_commits_linked` count in frontmatter. Mark PASS if matches.
    5. **SC-5 (0 token drift + 0 duplicate CTAs + 0 tab-bar-occluded):** Inspect buckets `token-drift`, `button-dup`, `tab-bar-occlusion`. Every entry must have a fixCommit OR be severity ≤ medium. Mark PASS.

    **Part B — 9-route visual spot-check:**

    Select 3 random routes per persona (9 total). Suggested sampling (your picks may differ):
    - Worker: `/home`, `/my/applications`, `/posts/<JOB_IDS[0]>`
    - Biz: `/biz`, `/biz/posts/<JOB_IDS[0]>/applicants`, `/biz/settlements`
    - Admin: `/admin`, `/admin/businesses`, `/admin/settlements`

    For each route:
    1. Start dev server: `npm run dev`
    2. Open in a real headed browser (not Playwright) at Desktop 1440×900 AND Mobile 375×812 viewports via browser dev tools
    3. Confirm substantive content renders (not skeleton, not blank, not stuck on loading)
    4. Click the primary CTA — confirm it does something sensible (navigation / modal / toast)
    5. Scroll to bottom on mobile — confirm no CTA is hidden under the tab bar (for non-HIDE_TAB_BAR routes)
    6. Record the route + verdict in the 09-REVIEW.md "Visual Spot-Check" section

    If ANY spot-check reveals a broken UI the automation did not catch:
    1. Append a new Phase9Issue entry directly to `.review/phase9-issues.json` (manually, following the Phase9Issue shape)
    2. Re-run `npx tsx scripts/review/phase9-issue-writer.ts --verify` — it MUST exit non-zero (new issue lacks fixCommit)
    3. Author fix commit, populate fixCommit field, re-run --verify
    4. Only return to Task 03-03 after --verify exits 0 again

    **Part C — Final REQUIREMENTS.md + STATE.md flip:**

    Use Edit tool:

    **REQUIREMENTS.md** — replace the 5 `in-progress` rows with `complete`:
    ```
    | QA-01  | Phase 9 | 09-02 | complete (YYYY-MM-DD, commit <closure-sha>) |
    | QA-02  | Phase 9 | 09-02 | complete (YYYY-MM-DD, commit <closure-sha>) |
    | QA-03  | Phase 9 | 09-02 | complete (YYYY-MM-DD, commit <closure-sha>) |
    | QA-04  | Phase 9 | 09-03 | complete (YYYY-MM-DD, commit <closure-sha>) |
    | QA-05  | Phase 9 | 09-02 | complete (YYYY-MM-DD, commit <closure-sha>) |
    ```
    Use today's date (`date '+%Y-%m-%d'`) and the planned closure commit short SHA (captured after Part D commit).

    **STATE.md** — update:
    - `## Phase Progress` table — change Phase 9 row to: `| 9. UI/UX Full Sweep (55 routes × Desktop + Mobile 375px) | Complete | <YYYY-MM-DD> |`
    - `## Current Position` — reflect Phase 9 closure; update `Phase` field and `Progress` percent
    - `progress.completed_phases` and `progress.completed_plans` frontmatter counters incremented
    - `last_updated` frontmatter to current ISO-8601 timestamp

    **Part D — Closure commit:**
    ```bash
    git add .review/phase9-issues.json \
      .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md \
      .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md \
      .planning/REQUIREMENTS.md \
      .planning/STATE.md
    git commit -m "docs(09): close Phase 9 — UI/UX Full Sweep signed off [qa-01..05]"
    git log -1 --format=%h  # capture the closure SHA; backfill into REQUIREMENTS.md rows as <closure-sha>
    ```

    Then amend REQUIREMENTS.md rows with the actual closure SHA and commit a second small fix-up:
    ```bash
    # edit REQUIREMENTS.md replacing <closure-sha> with the real short SHA
    git add .planning/REQUIREMENTS.md
    git commit -m "docs(09): backfill closure commit SHA in REQUIREMENTS traceability"
    ```
    OR author the closure commit with the SHA pre-known (via dry-run: author → amend). Either approach satisfies the acceptance criteria as long as REQUIREMENTS.md's final state contains a real short SHA.
  </how-to-verify>
  <files>
    .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .review/phase9-issues.json
  </files>
  <action>
    See <how-to-verify> above. Human reviewer attests all 5 ROADMAP Phase 9 Success Criteria (marks each PASS/FAIL in the 09-REVIEW.md attestation appendix with evidence refs), spot-checks 9 random routes (3 per persona) in a real headed browser at both Desktop + Mobile 375px, flips REQUIREMENTS.md QA-01..05 from in-progress to complete with closure commit SHA, updates STATE.md Phase Progress row to Complete, and authors the closure commit message `docs(09): close Phase 9 — UI/UX Full Sweep signed off [qa-01..05]`.
  </action>
  <verify>
    <automated>grep -q "^phase9_complete: true" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md &amp;&amp; grep -q "| QA-01 .* complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "| QA-04 .* complete" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "9. UI/UX Full Sweep.*Complete" .planning/STATE.md &amp;&amp; git log --grep="close Phase 9" --oneline | head -1</automated>
  </verify>
  <done>Resume-signal recorded: signed-off: all 5 SC PASS (phase closed) OR signed-off-with-deferrals OR rework/stop noted in STATE.md.</done>
  <resume-signal>
    Resume with ONE of:
    - `signed-off: all 5 SC PASS` — all 5 SC marked PASS + 9 routes spot-checked + REQUIREMENTS QA-01..QA-05 flipped to complete + closure commit authored. Phase 9 is closed; `/gsd:verify-work 9` can run next.
    - `signed-off-with-deferrals: <list>` — PASS with MOCK-LOG deferrals. Phase 9 closed with documented v1.2 carry-over.
    - `rework: <reason>` — spot-check or SC review surfaced a new blocker. Loop back to Task 03-01b.
    - `stop: <reason>` — Phase 9 cannot close in this session. STATE.md updated with blocker note before session end.
  </resume-signal>
</task>

</tasks>

<verification>
After all 4 tasks complete + signed off, run the Phase 9 closure gate sequence:

```bash
# 1. --verify exits 0
npx tsx scripts/review/phase9-issue-writer.ts --verify

# 2. 09-REVIEW.md has phase9_complete: true
grep -q "^phase9_complete: true" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md

# 3. All 5 SC rows attested (PASS or FAIL, not pending)
! grep -E "^\| [0-9] .* pending " .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-REVIEW.md

# 4. REQUIREMENTS.md QA-01..QA-05 flipped to complete
grep -q "| QA-01 .* complete" .planning/REQUIREMENTS.md
grep -q "| QA-04 .* complete" .planning/REQUIREMENTS.md

# 5. STATE.md Phase 9 row = Complete
grep -qE "9\. UI/UX Full Sweep.*Complete" .planning/STATE.md

# 6. No auto-fix-loop invocation in Phase 9 commits
git log --all --grep="auto-fix" | grep -i "phase 9\|fix(09)" && echo "FAIL: Pitfall 4 violated" || echo "OK"

# 7. 09-VALIDATION.md nyquist_compliant: true
grep -q "^nyquist_compliant: true" .planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-VALIDATION.md

# 8. Closure commit exists
git log --grep="close Phase 9" --oneline | head -1
```

If all 8 gates pass, Phase 9 is closed and milestone v1.1 can proceed with any remaining phases (Phase 7 DB apply + Phase 8 HUMAN-UAT if still open).
</verification>

<success_criteria>
- `npx tsx scripts/review/phase9-issue-writer.ts --verify` exits 0
- `09-REVIEW.md` exists with frontmatter `phase9_complete: true`, `critical_issues: 0`, `high_issues: 0`, `routes_swept: 54`, `viewports: [review-desktop, mobile-375]`
- All 5 ROADMAP Phase 9 Success Criteria rows in 09-REVIEW.md attestation appendix marked PASS (or FAIL + reason) — not `pending`
- Human reviewer recorded 9 spot-checked routes (3 per persona × worker/biz/admin) with verdicts in 09-REVIEW.md "Visual Spot-Check" section
- REQUIREMENTS.md traceability: QA-01..QA-05 status = `complete (YYYY-MM-DD, commit <short-sha>)` with real short SHA
- STATE.md Phase Progress: row "9. UI/UX Full Sweep ... | Complete | YYYY-MM-DD"
- STATE.md frontmatter counters (`completed_phases`, `completed_plans`) incremented
- 09-VALIDATION.md frontmatter `nyquist_compliant: true` + rows 09-03-01 + 09-03-02 ✅ green
- No commit in Phase 9 history invokes `scripts/review/auto-fix-loop.ts` (Pitfall 4 closure-level enforcement)
- Closure commit `docs(09): close Phase 9 — UI/UX Full Sweep signed off [qa-01..05]` recorded in git log
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-full-sweep-55-routes-desktop-mobile-375px/09-SUMMARY.md` (PHASE-level). Contents:
- Executive summary of what shipped across Plans 01 + 02 + 03
- Per-plan traceability (artifact list, test counts, commit SHAs)
- Issue counts by severity at open vs close
- Human fix commits linked (list of short SHAs + issue IDs)
- 5 Success Criteria attestation verbatim from 09-REVIEW.md
- Known carry-over (MOCK-LOG deferrals) to v1.2 if any
- Pointer to next phase (Phase 7 DB apply, Phase 8 HUMAN-UAT, or milestone close)
</output>
